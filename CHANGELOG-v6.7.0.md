# Trading-booooo v6.7.0-ONLINE-COIN-LEARNING

Base: v6.6.1-JOINT-OBJECTIVE.

## 확인된 문제

- 기존 LOB 교정은 매시간 패턴 단위로만 갱신됐다. 실시간·코인별 학습이 아니었다.
- 대시보드는 LOB 프로필이 아니라 구형 스캐너 프로필을 표시해 실제 교정 상태가
  보이지 않았다.
- 메이커 매수 체결 직후 동일한 매도 압력과 매수벽 감소를 반전으로 다시 읽어,
  한 번의 관측만으로 시장가 청산할 수 있었다.
- 진입 때 저장한 `dynamicStatus`를 실시간 상태처럼 재사용하는 경로가 있었다.
- v6.6이 사용자 승인 없이 자본 슬롯을 6개에서 12개로 늘려 주문액이 절반
  수준으로 축소됐다.

## 실시간 코인 학습

- 모든 종료 실거래를 `lob_online_outcomes`에 한 번만 기록한다.
- 거래 종료 직후 `exchange × coin × pattern` 프로필과 상위
  `exchange × pattern` 프로필의 버전을 증가시킨다.
- 수익거래율, 목표 적중률, 순손익 bp, 보유시간, MAE/MFE, 수익 거래의 MAE,
  20초 미만 조기손실, 청산 사유를 계속 갱신한다.
- 코인 표본은 상위 패턴 프로필로 수축해 한두 번의 결과에 과적합하지 않는다.
- 불리한 코인 통계는 후보 우선순위와 확인 횟수에만 반영하며 거래 후보를
  삭제하지 않는다.
- 수익 거래의 정상 역행폭은 손절 하한에 반영하되, 넓어진 손절로 비용 포함
  순EV를 다시 계산하고 양수가 아니면 진입하지 않는다.
- 실시간 업데이트가 실패한 거래는 시간별 교정 작업이 멱등 백필한다.

## 조기청산 교정

- 하드 손절·목표·절대 시간종료는 즉시 유지한다.
- 일반적인 LOB 지지 붕괴·반전은 메이커 체결 안정화 시간과 2~4회 연속 확인을
  거친다.
- 극단적 스프레드·깊이 붕괴·강한 동시 매도압력은 즉시 청산한다.
- 목표 도달과 무효화가 동시에 관찰되면 목표 적중을 우선 기록한다.
- 진입 시점의 오래된 `dynamicStatus`는 청산 판단에 재사용하지 않는다.

## 자본 배분 원복

- `scalp_position_slots`를 12개에서 기존 6개로 복구한다.
- 후보 검색 범위는 넓게 유지한다.
- 온라인 학습은 슬롯 수, 총 배분액, 포지션당 자금처럼 사용자가 정하는 자본
  권한을 변경하지 않는다.

## 가시성

- 대시보드 자기교정 카드에 온라인 LOB 프로필 버전, 실시간 갱신 방식,
  수익거래율, 순손익, 학습 거래·코인 수, 평균 보유시간을 표시한다.
- `SQL_DIAGNOSE_SHORT_LOSSES_V670.sql`로 각 거래의 실제 조기청산 사유와
  온라인 프로필을 검증할 수 있다.
