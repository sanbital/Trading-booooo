# Trading-booooo v6.6.1-JOINT-OBJECTIVE

Base: v6.6.0-LIVE-DATA.

## 확인된 회귀

- 배포 직후 실시간 LOB 후보 17건이 모두 `NET_EV_NOT_POSITIVE`로 폐기됐다.
- 평균 EV 하한은 -4.21bp였다.
- 패턴별 평균 EV는 OFI -4.30bp, ABSORPTION -4.68bp, SWEEP -1.69bp였다.
- 하루치 저표본 패턴 교정이 진입 권한을 직접 소유해 거래 회전율을 0으로 만든
  것이 원인이었다.

## 공동 목적함수

- 기본 양수 순EV 문턱은 그대로 유지한다.
- 패턴 실거래 학습은 즉시 후보 우선순위에 반영한다.
- 불리한 패턴 학습값은 표본 수와 관계없이 진입을 차단하지 않고 우선순위·회전에만
  반영한다. 승률을 거래 삭제로 높이는 경로를 구조적으로 금지한다.
- 유리한 패턴 증거만 120~360표본에서 하드 게이트 영향력을 점진 확대한다.
- 후보는 순EV, 실현 수익거래 비율, 슬롯 초당 기대이익의 비지배 전선으로 정렬한다.
- 정렬은 후보를 제거하지 않으며 거래횟수 상한도 추가하지 않는다.
- 패턴별 실현 중앙 보유시간을 회전율 계산에 반영한다.

## 리컨실링 복구

- 수량 0·phase 없음·`EXCHANGE_OPEN`인 구버전 메이커 주문을 ENTRY_PENDING으로
  복구한다.
- 다음 모니터가 거래소 주문을 직접 조회·취소하고 실제 체결분만 회계 반영한다.
- DB만 지워 거래소 주문을 고아 상태로 만드는 동작은 하지 않는다.
