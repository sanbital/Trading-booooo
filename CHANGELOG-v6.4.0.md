# v6.4.0-HEAT

## 왜 멈췄는가 — 코드에서 확인된 원인

### 1. 불일치 자산은 청산 평가를 통째로 건너뛰고 있었음 (ETH 8h39m의 원인)

`market-autotrader/index.ts` 감시 루프:

```ts
if (!position.is_paper && mismatches.has(position.base_asset)) { ...; continue; }
```

계좌 불일치가 잡힌 자산은 시세 조회도, 손절·목표·타임아웃 판단도 하지 않고
넘어갔다. 진입만 막는 게 아니라 **보유 물량의 관리 자체가 정지**됐다.
업비트 ETH가 여기 걸려 8시간 39분 방치됐고, 같은 시각 다른 종목은 정상 거래됐다.

→ 불일치 판정은 **신규 진입만** 막는다. 청산은 항상 수행하고, 매도 수량만
실제 인출 가능 잔고로 제한한다.

### 2. 미체결 주문 조회 실패가 "주문 없음"으로 처리됨 (LTC의 원인)

```ts
} catch {
  return new Map();   // "못 읽었다"와 "하나도 없다"가 같은 값
}
```

게이트웨이가 한 번만 흔들려도 봇 자신의 예약익절이 사용자 수동 잠금으로 둔갑했다.

→ 실패 시 `null`(모름)을 반환한다. 원인을 특정할 수 없으면 아무도 탓하지 않고,
그 사이클의 잠금 판정을 보류한다.

### 3. 시세 미수신이 무성 실패였음

`if (!(current > 0)) continue;` — 로그 한 줄 없이 넘어갔다. 시세가 끊긴 포지션은
아무 흔적 없이 무기한 방치될 수 있었다.

→ `MARKET_DATA_UNAVAILABLE` 이벤트 + 연속 카운터. 4회부터 CRITICAL.

### 4. scalp-calibration을 호출하는 것이 아무것도 없었음

v5.9에서 교정 주기를 6시간 → 1시간으로 줄이고 Platt 최소표본을 200 → 60으로
낮췄다. 함수는 배포 목록에 들어갔지만 **스케줄도, 트리거도, 내부 호출자도
없었다.** 실패하지 않았기 때문에 드러나지 않았다. pWin 교정은 표본이 아무리
쌓여도 사전분포 그대로 돌고 있었다.

→ `.github/workflows/scalp-calibration.yml` 신설 (매시 12분).
lob-calibration은 매시 42분으로 정상 작동 중이었다.

### 5. CI가 절반의 테스트를 돌리지 않고 있었음

`deno.json`의 test 태스크에 `_shared/**` 가 빠져 있고 `--allow-read`도 없었다.
버전 일치 검사(version.test.ts)와 마이그레이션 가드(migrations.test.ts)가
**한 번도 실행되지 않았다.** 실제로 게이트웨이 6.3.1 / 엔진 6.3.3으로 이미
어긋나 있었다.

→ test 태스크 복구. 버전 상수 3개 + 대시보드 배너를 6.4.0-HEAT로 정렬.

---

## 10달러 미만 오인 금지

대조 판정을 `core.ts`의 `reconcileAccount()` 순수 함수 하나로 통합했다.
허용오차는 다음의 최댓값이다.

- 수량 스텝 × 2
- 왕복 수수료 규모 × 3
- **금액 기준 먼지 한도** — 업비트 14,000원 / 바이낸스 10 USDT

### 하나 추가한 안전장치

절대 금액 기준이 **포지션 수량의 50%를 넘지 못하게** 상한을 뒀다.
10달러 기준을 12달러짜리 포지션에 그대로 적용하면 포지션 전체가 소리 없이
사라질 수 있기 때문이다. 정상 크기 포지션에서는 이 상한이 걸리지 않는다.

### 판정 결과

| 판정 | 상황 | 신규매수 | 청산 |
|---|---|---|---|
| `MATCH` | 일치 | 허용 | 허용 |
| `DUST_ALIGN` | 먼지 차이 → 장부 자동정렬 | 허용 | 허용 |
| `UNKNOWN_LOCK` | 미체결 조회 실패 → 판단 보류 | 허용 | 허용 |
| `ASSET_REVIEW` | 봇 것이 아닌 잠금 | **차단** | 허용 |
| `VANISHED` | 실제로 계좌를 떠남 | **차단** | 허용 |

`allowExit: true`는 타입에 상수로 박아 뒀다. 청산이 막히는 경우가 없다는 것이
누락된 검사가 아니라 명시된 계약이 되도록.

---

## 자기교정

- `scalp_auto_tune_edge_budget` 기본값 `false` → `true`. 적용 코드는 v5.9부터
  이미 있었고 스위치만 꺼져 있었다.
- 기존 안전장치는 그대로다: 점추정이 아닌 신뢰구간 하한, `n/(n+400)` 수축,
  클램프.
- 교정 주기: **scalp 매시 12분 / LOB 매시 42분.** 같은 청산 표본을 읽으므로
  같은 분에 돌지 않도록 분리했다.

---

## 신규 CI 가드

- `scheduling.test.ts` — 배포되는 함수에 호출자(스케줄·트리거·내부 호출)가
  반드시 있어야 한다. 4번 항목이 다시 발생하면 배포가 실패한다.
- 두 교정 워크플로의 cron 분이 겹치지 않는지 검사.
- `core.test.ts`에 대조 판정 테스트 7개 추가 (LTC·ETH 실제 수치 사용).

---

## 검증 상태

- 수정 파일 전부 구문 검사 통과 (esbuild)
- 모든 워크플로 YAML 파싱 통과
- `migrations.test.ts` 두 규칙, `scheduling.test.ts` 두 규칙을 별도로 재현해
  통과 확인
- **`deno test` 자체는 실행하지 못했다.** 이 환경에서 deno.land 접근이 차단돼
  있다. 배포 전에 `deno task test`를 반드시 한 번 돌려야 한다 — 이번에
  `_shared/**` 가 처음으로 CI에 포함되므로, 그동안 실행되지 않던 테스트에서
  기존 실패가 드러날 수 있다.

## 배포 후 확인

1. 대시보드 배너가 `v6.4.0-HEAT`인지 — 아니면 배포가 중간에 실패한 것
2. `LEDGER_FEE_DUST_HEALED` 는 늘고 `ASSET_SCOPED_PAUSE` 는 줄어야 정상
3. `MARKET_DATA_UNAVAILABLE` — 그동안 안 보이던 실패가 처음 드러날 것
4. Actions 탭에 Scalp Calibration이 매시 12분에 도는지
5. 오늘 08:23 수동 ETH 매도는 정당한 불일치다. 이 정지는 정상이며
   마이그레이션이 해제한다
