# Trading-booooo v3.2.0 — Event-aware Profit Gate

## 목적
추천 종목이 비용 차감 후 수익을 낼 가능성을 높이기 위해 가격·호가 데이터만으로 설명되지 않는 급등 원인을 뉴스·공시·언락 데이터로 검증합니다.

## 판정
- `BLOCK`: 해킹·익스플로잇·상장폐지·거래중단·고위험 언락. 최종 결정 `AVOID`.
- `REVIEW`: 일반 언락·신규 상장·규제·재단 이동·네트워크 이벤트 또는 원인 불명 거래대금 급증. 최종 결정 최대 `WAIT`.
- `CLEAR`: 활성 공급자에서 중대 이벤트 미검출. 다른 모든 게이트를 통과할 때만 `BUY`.
- `UNKNOWN`: 공급자 미설정 또는 전부 실패. 라이브 `BUY` 금지.

## 공급자
1. NewsAPI: 최근 96시간 기사 제목·설명 검색
2. CoinMarketCal API v2: 최근/향후 14일 이벤트, 표시 날짜와 영향도 사용
3. EVENT_FEED_URL: 거래소 공지·온체인 재단지갑·보안 피드 등 사용자 정의 정규화 피드

## 안전장치
- 공급자별 OK/DISABLED/ERROR 상태 노출
- 기사 키워드만으로 중대 판정하지 않고 카테고리·신뢰도 기준 병행
- 4시간 거래대금비 8배 또는 일봉 20배 이상인데 근거 이벤트가 없으면 `REVIEW`
- 이벤트 증거와 판정 상태가 각 후보 JSON 및 리포트에 저장
- 이벤트 공급자는 15분 캐시로 호출량과 지연 제한

## 남은 검증
역사 뉴스 데이터가 없는 캔들 백테스트에서는 이벤트 알파를 소급해 꾸며내지 않습니다. 이벤트 게이트의 성과는 앞으로 축적되는 라이브/포워드 신호에서 `BLOCK/REVIEW/CLEAR`별 순수익과 놓친 기회율을 별도로 검증해야 합니다.
