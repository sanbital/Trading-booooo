# v5.9.0 — 자기교정이 실제로 시작되게 만들기

## 방침

**손실을 줄이려고 거래를 줄이지 않는다. 거래를 늘리면서 맞서 싸우고, 승률은
자기교정으로 올린다.**

이 방침과 정면으로 충돌하는 구조가 있었습니다.

## 문제: 자기교정이 영원히 시작되지 않았다

캘리브레이션 루프가 **실제 체결된 포지션만** 표본으로 썼습니다.

```
관측된 실제 진입 속도   = 2시간에 1건
필요 표본               = 200건
도달 시간               = 400시간 = 17일   (거래가 계속 나온다는 가정 하에)
```

즉 `edgeBudget` 은 계속 추정치로 남고 → 배리어 폭이 틀린 채로 유지되고 →
게이트가 계속 후보를 거절합니다. **거래를 못 하게 만드는 조건 자체를 스스로
교정할 수 없는 구조**였습니다.

그런데 스캐너는 이미 매 스캔마다 **결선 후보 전체**(BUY 아닌 것 포함)를
`scanner_candidates` 에 저장하고 있었습니다.

```
결선 8종목 x 거래소 2개 x 시간당 60스캔 = 시간당 960건
실제 체결                                = 시간당 0.5건
                                           → 1,920배
```

**증거는 이미 디스크에 쌓이고 있었고, 아무도 읽지 않았습니다.**

## 해결: 섀도우 평가

각 후보에 대해 그 뒤 가격 경로를 되짚어, 어느 배리어를 먼저 건드렸을지 판정합니다.
그러면 이 설계 전체가 기대고 있는 단 하나의 숫자가 나옵니다.

```
delta = 실현 목표선도달률 − 중립승률 S/(T+S)
```

**`edgeBudget` 이 바로 이 값이고, 배리어 폭은 2C/delta 입니다.**
지금까지 0.10 으로 가정만 하고 검증할 방법이 없던 값입니다.

측정되면 → 배리어가 맞아지고 → 게이트가 열리고 → 거래가 늘어납니다.
방침이 요구한 자기교정 루프가 이제 닫힙니다.

- `200표본까지 17일` → **약 12분**
- 캘리브레이션 주기 6시간 → **1시간**
- Platt 보정 최소표본 200 → **60** (수축이 이미 보호. n=60에서 적합 가중치 17%)

### 섀도우가 측정하지 않는 것

체결 위험·슬리피지·메이커 대기열이 **의도적으로 빠져 있습니다.** 섀도우는
"신호가 방향을 맞히는가" 만 격리해서 봅니다. 그게 pWin 이 주장하는 것이고
`edgeBudget` 이 표현하는 것입니다. 체결 품질은 실제 체결분에서 따로 측정합니다.

### 보수적으로 설계한 지점

- **점추정치가 아니라 신뢰구간 하한**을 씁니다. delta 를 과대평가하면 배리어가
  좁아지고 신호에 없는 엣지를 요구하게 됩니다 — 손으로 0.10 을 박았던 것과
  같은 실패를, 단계만 더 밟아서 반복하는 셈입니다. 과소평가는 회전이 줄 뿐입니다
- 한 바에서 목표·손절을 모두 건드리면 **손절로 기록**합니다. OHLC 로는 순서를
  알 수 없고, 유리한 쪽을 가정하면 측정하려는 그 숫자가 부풀려집니다
- `n/(n+400)` 수축으로 한 구간이 기하구조 전체를 흔들지 못하게 합니다
- **자동 반영은 기본 OFF** (`scalp_auto_tune_edge_budget`). 측정과 보고는 항상,
  설정 덮어쓰기는 사장님이 켜실 때만

## 신규 (`202607260011_shadow_edge_measurement.sql`)

| 항목 | 내용 |
|---|---|
| `scalp_shadow_outcomes` | 후보별 배리어 판정 결과 |
| `scalp_edge_budget_source` | `ASSUMED` / `MEASURED` |
| `scalp_edge_budget_measured_at` · `_samples` | 측정 시각·표본 수 |
| `scalp_auto_tune_edge_budget` | 측정값 자동 반영 (기본 false) |

## 부수 수정

`ENTRY_CIRCUIT_BLOCK` 의 `new entries are paused` 한 문장이 **세 가지 다른 상황**
(운영자 정지 / 출금 모드 / 계좌 대조 미해결)을 덮고 있어, 로그만으로는 원인을
구분할 수 없었습니다. 각각 다른 문구로 분리했습니다.

## 검증

CI 동일 명령, 타입검사 포함 **193개 + 마이그레이션 가드 3 + 버전 가드 2 통과 / 0 실패.**
(섀도우 14개 신규)

## 처음 돌리실 때

```
1. 마이그레이션 011 적용 → 함수 배포
2. scalp-calibration 을 workflow_dispatch 로 dry_run=true 수동 실행
3. 응답의 shadow 블록 확인:
     resolved            측정에 쓰인 표본 수
     realized_win_rate   실제 목표 선도달률
     neutral_win_rate    무알파 기준선
     measured_edge       ← 이게 진짜 delta
     predicted_edge      모델이 주장하던 값
```

`measured_edge` 와 `predicted_edge` 의 차이가 **모델이 자기 자신을 얼마나
과대평가하고 있었는지**입니다. 이 숫자를 보신 뒤에
`scalp_auto_tune_edge_budget` 을 켤지 정하시면 됩니다.
