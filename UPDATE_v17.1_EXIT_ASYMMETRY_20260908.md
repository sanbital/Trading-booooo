# V17.1 — 손실 비대칭 교정 (2026-09-08 실거래 근거)

`LEADER_MOMENTUM_V17` 청산 정책(`supabase/functions/_shared/leader-momentum-v17.mjs`)에
**비용 상한(cost cap)** 과 **무진행 청산(no-progress)** 두 계층을 추가한다.
진입 로직·사이징·주문 경로는 바꾸지 않는다. 순수 함수 `nextExit` 변경만이다.

## 1. 오늘 거래 데이터

바이낸스 USD-M, 3배, 명목 약 120 USDT/건, 10건 전량 청산됨.
출처: `v11_long_regime_positions`, `v11_long_regime_decisions`, `exchange_trade_fills`.

| # | 심볼 | MFE | 실현 | 청산 사유 | 보유 |
|---|------|-----|------|-----------|------|
| 1 | INJUSDT | +0.74% | **-2.60%** | HARD_STOP | 32분 |
| 2 | SOPHUSDT | +6.00% | +3.90% | TRAILING | 42분 |
| 3 | IOSTUSDT | +1.20% | **-3.53%** | HARD_STOP | 10분 |
| 4 | AEROUSDT | +1.11% | **-3.10%** | HARD_STOP | 60분 |
| 5 | SOPHUSDT | +9.45% | +6.18% | TRAILING | 15분 |
| 6 | USELESSUSDT | +2.12% | **-2.82%** | HARD_STOP | 16분 |
| 7 | AKEUSDT | +0.60% | **-3.53%** | HARD_STOP | 24분 |
| 8 | FORMUSDT | +0.00% | **-4.75%** | HARD_STOP | 7분 |
| 9 | BTRUSDT | +4.42% | +1.75% | TRAILING | 26분 |
| 10 | USELESSUSDT | +5.03% | +0.68% | TRAILING | 27분 |

승률 40%, 실현 합계 **-10.62 USDT**.

## 2. 진단

### 2.1 손실 6건 중 5건은 한때 수익이었으나 아무 보호도 받지 못했다
`trailArmPct=.03`이 유일한 보호 장치였다. 손실 6건의 MFE는 +0.00% ~ +2.12%로
전부 3% 미만이라 트레일링이 한 번도 무장되지 않았고, 전부 -2.5% 하드스톱까지 갔다.
특히 USELESSUSDT(06:16)는 **+2.12%까지 갔다가 -2.82%로 마감**했다.

### 2.2 죽은 포지션을 하드스톱까지 들고 있었다
INJ(32분), AERO(60분), AKE(24분)는 ±1% 안에서 횡보하다가 마지막에 무너졌다.
모멘텀 진입인데 모멘텀이 나오지 않은 상태였고, `staleMs=45분`은 너무 느려 발동하지 않았다.
AERO는 60분 중 40분을 0% 부근에서 보낸 뒤 손절됐다.

### 2.3 청산 감시가 60초에 한 번이다 (구조적 한계)
`cron.job` `v11-long-regime-executor` 스케줄은 `* * * * *`이다. 틱 사이는 무방비다.
`v11_long_regime_decisions`의 분당 호가 기록이 이를 직접 보여준다.

- **FORMUSDT** 07:51 호가 -1.07% → 07:52 호가 **-4.80%**. 60초 만에 -2.5% 스톱을 2.3%p 관통.
- **USELESSUSDT** 10:26 호가 **+5.03%**(이 틱에서 트레일 무장, 스톱 +3.48%) → 10:27 호가 **+0.78%**.
  스톱 가격은 한 번도 체결 가능한 적이 없었다.

체결가는 관측 호가와 거의 일치했다(차이 대부분 0.1%p 미만). 즉 **주문 슬리피지가 아니라
관측 공백**이 원인이다. 스톱 레벨 대비 체결가 이탈은 10건 평균 **-1.12%p**, 합계 약 -13.5 USDT로
당일 손실 전체보다 크다.

## 3. 변경 내용

```js
lockArmPct: .02, lockGivebackPct: .004,      // 비용 상한
noProgressMs: 20*60_000, noProgressMfePct: .015,  // 무진행 청산
```

- **비용 상한 (`V17_COST_CAP_STOP`)** — MFE가 2% 이상 나온 적이 있으면 스톱 바닥을
  `entry*(1-.004)`로 올린다. 한 번 2% 수익을 보여준 거래는 다시 -2.5% 손실이 될 수 없다.
- **무진행 청산 (`V17_NO_PROGRESS`)** — 진입 후 20분이 지났고, 최대 MFE가 1.5% 미만이며,
  **현재 손실 중일 때** 원가 부근에서 정리한다. 세 조건을 모두 요구하므로 조용히 잘 가고 있는
  포지션은 시간만으로 잘리지 않는다.
- 세 바닥(하드스톱·비용상한·트레일링)은 `oldStop`을 포함해 **오직 상승만 하는 래칫**이다.
  정책 파라미터가 계층 순서를 깨면 `INVALID_EXIT_STATE`로 거부한다.

## 4. 검증

`v11_long_regime_decisions`에 기록된 분당 호가 259틱을 **실제 배포 코드로** 재생했다
(`git HEAD` 구버전 = 기준선, 작업본 = V17.1). 기준선 재생은 10건 전부 실제 청산 틱·사유와
정확히 일치했다.

| | 기준선 | V17.1 | 차이 |
|---|---|---|---|
| 가격수익 합계 | -7.24%p | **+3.89%p** | **+11.13%p** (명목 120 USDT 기준 약 **+13.4 USDT**) |

건별: INJ +2.42%p, AERO +2.91%p, USELESS(06:16) +2.43%p, AKE +3.37%p.
**손해를 본 거래는 0건이다.** 승리 4건의 청산 시점·가격은 전혀 바뀌지 않았다.

민감도: `noProgressMs` 18/20/22분 → +9.17/+8.70/+9.12%p, `noProgressMfePct` 1.2~2.0% 구간 전부
동일(+8.70%p)로 평탄하다. Leave-one-out 시 어떤 거래를 빼도 +7.76 ~ +11.13%p가 남아
단일 거래에 의존하지 않는다.

## 5. 채택하지 않은 것 (근거 부족)

- **조기 손절(진입 직후 미회복 시 청산)** — FORMUSDT는 살리지만 BTRUSDT를 죽인다.
  BTR은 -1.79%까지 밀렸다가 +4.42%로 돌아섰다. 반례가 있어 기각.
- **MFE 2.5% 이상에서 익절 고정(2단 락)** — 오늘 데이터에서 한 번도 발동하지 않았다.
  검증되지 않은 복잡도라 넣지 않았다.
- **분할 익절** — 재생상 약 +2.6%p이나, 실제 청산 이후 구간은 데이터가 없어(censored)
  결론을 낼 수 없다.

## 6. 남은 문제: 60초 감시 주기

FORMUSDT(-4.80%)는 이번 변경으로 개선되지 않는다. 7분 만에 한 틱에서 스톱을 관통했고,
분당 폴링으로는 볼 수 없는 움직임이다. 근본 해결은 둘 중 하나다.

1. **거래소 상주 스톱 주문** — `STOP_MARKET reduce_only`를 스톱 가격에 걸어두고 래칫 시 정정.
   틱 단위로 거래소가 집행하므로 관측 공백이 사라진다. 다만 현재 게이트웨이는
   `gateway/server.mjs:1197`에서 주문 타입을 `LIMIT/MARKET/LIMIT_MAKER`로 제한하고 있어
   게이트웨이 변경과 미체결 주문 정합성(고아 주문·청산 시 취소) 설계가 선행되어야 한다.
2. **감시 주기 단축** — `cron.job` 스케줄을 15초로. pg_cron 초단위 문법을 이미 쓰고 있다.
   포지션당 게이트웨이 호출이 4배가 되므로 레이트리밋 검토가 필요하다.

두 방안 모두 실행 경로를 건드리므로 이번 변경(순수 정책 함수)과 분리했다.

## 7. 테스트

`supabase/functions/_shared/leader-momentum-v17.test.ts` 12개 신규 (전부 통과).
기존 스위트 895건 통과. 실패 1건(`p10-regime-router-v10.test.ts`)은 본 변경 이전부터
존재하던 것으로, 변경 전 트리에서도 동일하게 실패함을 확인했다.
