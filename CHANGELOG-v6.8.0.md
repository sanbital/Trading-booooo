# Trading-booooo v6.8.0 — VALIDATED PARETO LEARNING

Base: v6.7.0-ONLINE-COIN-LEARNING.

## 왜 바꿨는가

v6.7은 실거래 종료 즉시 코인·패턴 통계를 갱신했지만, 통계 갱신과 실전 모델
승격이 분리되지 않았다. 표본 수가 모델 버전처럼 표시됐고, 새 통계가 실제로
승률·순수익·거래 회전율을 함께 개선했는지 검증하는 대조군도 없었다.

2026-07-27 제공 실거래 67건은 다음 문제를 보였다.

- 순이익 거래 비율 23.9%, 평균 순손익 -14.88bp
- 예측 `p_target` 평균 51.5%에 비해 실제 목표 도달률 11.9%
- 예측 체결률 평균 78.0%에 비해 메이커 실현 체결률 39.2%
- 58건이 STOP, TARGET_2는 8건
- 모든 주요 패턴의 평균 순손익이 음수

따라서 “계속 숫자를 바꾸는 학습” 대신 “동시에 개선됐다고 실거래로 입증된
정책만 실전에 남는 학습”으로 변경했다.

## 모델 수명주기

1. 모든 실거래 종료 결과는 원시 학습 원장에 즉시 기록한다.
2. 운영 중인 CHAMPION 정책은 불변 스냅숏으로 고정한다.
3. 20건 이상의 새 결과가 쌓이면 새 통계를 CHALLENGER 정책으로 동결한다.
4. 한 스캔 전체를 결정론적으로 CHAMPION 또는 CHALLENGER에 50:50 배정한다.
5. 두 정책 모두 동일한 정상 주문금액·동일 슬롯·동일 자본 배분으로 거래한다.
6. 공동 승격 조건을 모두 만족한 CHALLENGER만 잠정 CHAMPION이 된다.
7. 잠정 CHAMPION은 이전 CHAMPION을 CONTROL로 둔 두 번째 독립 실거래 검증을
   다시 통과해야 확정된다.
8. 재검증에 실패하면 이전 CONTROL을 자동 복구한다.

원시 프로필은 더 이상 `active=true`로 자동 전환되지 않는다. 대시보드의 `P번호`는
표본 수가 아니라 실제 불변 정책 버전이다.

## 공동 승격 조건

기본 검증은 정책별 종료 거래 40건과 배정 스캔 20건 이상에서 시작한다.
다음 조건을 하나라도 통과하지 못하면 승격하지 않는다.

- 수수료 포함 평균 순손익이 양수
- 주문금액 가중 순손익과 기하평균 순손익도 양수
- Profit Factor > 1
- CHAMPION 대비 순손익 개선의 단측 90% 신뢰 하한 > 0
- CHAMPION 대비 승률 개선의 단측 90% 신뢰 하한 > 0
- 스캔당 완료 거래 수 감소 없음
- 슬롯 시간당 완료 거래 수 감소 없음
- 자본 시간당 회전 수 감소 없음
- 평균 주문금액이 CHAMPION의 98% 이상
- 최대 낙폭이 CHAMPION의 105% 이하
- 양 정책 공통 `거래소 × 패턴` 표본이 전체의 80% 이상이고, 동일 구성으로
  표준화한 승률과 순손익도 모두 개선

20건씩 쌓인 시점에 통계적으로 명백히 나쁘면 조기 거절한다. 정책별 120건까지
공동 우위가 입증되지 않으면 평가 예산 만료로 거절 또는 롤백한다.

## 회전율·주문금액 방어

- `scalp_position_slots`, 총 운용 배분, 보호금, 주문 상한, 손실 한도를 변경하지 않는다.
- 작은 주문으로 수익률을 꾸미지 못하도록 평균 실투자금 비율을 검증한다.
- 거래를 줄여 승률을 꾸미지 못하도록 스캔당 거래 수와 슬롯 회전율을 검증한다.
- 짧은 손실 거래로 회전율만 높이지 못하도록 양의 순손익·Profit Factor·승률
  개선을 동시에 요구한다.
- 거래소·패턴 구성 차이로 생기는 심슨의 역설을 구성 표준화 게이트로 차단한다.
- KRW와 USDT를 직접 합산하지 않는다. 주문금액 가중수익·자본회전·정상
  주문금액은 거래소 안에서 먼저 계산한 뒤 통화중립적으로 결합한다.

## 런타임 안전성

- 정책 스냅숏은 후보 정렬부터 주문 직전 재검사까지 고정한다.
- 포지션의 소프트 청산 조건도 진입 정책 버전에 고정한다.
- 거절·롤백 직후 퇴역 모델이 한 번 더 진입하지 않도록 전역 정책 캐시를 제거했다.
- 정책 테이블을 읽지 못하면 미검증 원시 통계로 폴백하지 않고 신규 LOB 진입만
  차단한다. 기존 포지션의 하드 손절·목표·진입 시 고정 청산 조건은 계속 감시한다.
- 승격·롤백은 advisory lock, 예상 버전 확인, 단일 CHAMPION/ALTERNATE 고유
  인덱스로 경쟁 상태를 방어한다.

## 가시성

자기교정 카드에 다음을 표시한다.

- 현재 CHAMPION 정책 버전과 검증 여부
- CHALLENGE / CONFIRMATION / IDLE 단계
- CHALLENGER 또는 CONTROL 버전과 실거래 배정 비율
- 즉시 갱신되는 원시 표본과 검증 후 적용되는 정책의 차이
- 승격의 공동 목표: `승률↑ · 순익↑ · 거래·회전 유지`

## 중요한 한계

이 릴리스는 손실을 수익으로 보장하는 “완성된 최적 파라미터”가 아니다. 제공된
과거 원장에는 미체결 후보의 이후 호가 경로가 없어서 새 정책을 같은 시장에서
정확히 재생할 수 없다. 이 버전이 검증한 것은 손실 모델을 자동 채택하지 않는
수명주기, 공동 목표 계산, 상태 전이, 롤백, 자본 설정 불변성이다. 실제 성과
개선 여부는 배포 후 동시 실거래 cohort가 결정한다.
