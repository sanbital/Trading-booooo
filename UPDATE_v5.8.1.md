# v5.8.1 — 거래량 급감 원인 수정 (EV 문턱이 설계 자체를 거부하고 있었음)

## 원인

v5.6 에서 배리어를 **처리량 최적점** `W* = 2C/δ` 로 바꿨습니다. 이 지점의 정의상
거래당 기대값은 **정확히 C** 입니다. 그런데 EV 게이트의 문턱 `minimumEdge` 는
0.10% 절대값 그대로 뒀습니다.

```
메이커 왕복 실비용 C     = 0.080%
설계상 거래당 EV = δW − C = 0.080%   ← 최적점의 산출물
EV 게이트 문턱           = 0.100%   ← 그보다 크다
```

**설계가 만들어내는 최댓값이 문턱보다 작습니다.** 가정한 10pp 엣지를 정확히
내는 신호조차 영구 차단됩니다.

| 신호 엣지 | pWin | 필요 해결확률 | v5.8.0 | v5.8.1 |
|---|---|---|---|---|
| +5pp | 53.0% | — | 영구 차단 | 영구 차단 |
| +8pp | 56.0% | 78% | **영구 차단** | 통과 가능 |
| +10pp | 58.0% | 63% | **영구 차단** | 통과 가능 |
| +15pp | 63.0% | 42% | 통과 가능 | 통과 가능 |
| +20pp | 68.0% | 31% | 통과 가능 | 통과 가능 |

+15pp 미만이 전부 막혀 있었으니 거래가 급감한 게 당연합니다.

## 수정

문턱의 목적은 **비용 추정 오차를 흡수**하는 것이므로, 다른 기하구조를 기준으로 정한
절대값이 아니라 **비용에 비례**해야 합니다.

```
throughput 모드:  minimumEdge = C × minEdgeCostFraction   (기본 0.25 → 0.020%)
legacy 모드:      minimumEdge = 기존 절대값 (변경 없음)
```

`resolveMinimumEdge()` 를 스캐너 신호 게이트와 주문 직전 EV 게이트 **양쪽에** 배선했습니다.
둘이 서로 다른 문턱을 쓰고 있으면 같은 문제가 재발합니다.

### 회귀 테스트

- `the EV threshold cannot exceed what the optimum produces` — 수수료 3종에서
  설계 EV = C 임을 확인하고, 문턱이 그보다 낮은지 검사
- `a signal at exactly the assumed edge can pass` — 가정한 엣지의 신호가
  도달 가능한 해결확률로 통과하는지 검사
- 문턱이 비용에 비례하는지, legacy 모드는 절대값을 유지하는지

## 거래 급감의 나머지 요인

이 버그가 가장 크지만 단독은 아닙니다. 로그로 구분하십시오.

| 확인할 것 | 무엇을 뜻하는가 | 대응 |
|---|---|---|
| `SCALP_GATE_BLOCK` 의 `reason` 분포 | `edge_below_minimum` 이 대부분이면 이번 버그 | v5.8.1 로 해소 |
| `MAKER_ENTRY_RESTED` 대비 `MAKER_ENTRY_FILLED` | 체결률. 낮으면 주문이 안 채워지는 것 | TTL·드리프트 완화 또는 하이브리드 |
| `MAKER_ENTRY_UNFILLED` / `MAKER_ENTRY_DRIFTED` 비율 | 호가가 계속 달아나는 상황 | `scalp_maker_entry_drift_ticks` 상향 |
| 열린 포지션의 평균 보유시간 | v5.8 로 시간청산이 없어져 슬롯이 오래 묶임 | 슬롯 수 상향 또는 흐름 반전 민감도 상향 |
| 후보 자체가 안 나옴 | 변동성 하한(도달예상 45분 ⇒ ATR 0.44%)에 걸림 | `SCALP_MAX_HOLDING_MINUTES` 상향 |

pWin 을 중립승률 기준으로 바로잡은 v5.7 도 통과율을 낮춥니다. 그건 버그가 아니라
**처음으로 확률을 정직하게 매기는 것**이고, 되돌릴 대상이 아닙니다.

## 신규 설정

| 컬럼 | 기본값 | 의미 |
|---|---|---|
| `scalp_min_edge_cost_fraction` | 0.25 | EV 문턱 = 왕복비용 × 이 값 |

마이그레이션 `202607260010` 에 추가했습니다.

**검증:** 타입검사 포함 **178개 + 마이그레이션 가드 3 + 버전 가드 2 통과 / 0 실패.**
(버전 가드가 이번에 실제로 드리프트를 잡아냈습니다.)

---

# v5.8.0 — 시간 기반 청산 완전 제거

## 원칙

**포지션은 시장이 닫는다. 시계는 아무것도 닫지 않는다.**

청산 사유는 이제 전부 시장 데이터입니다.

| 사유 | 근거 |
|---|---|
| 목표 도달 | 예약 지정가 매도 체결 |
| 손절 / 추적손절 | 가격 |
| `edge_negative` | 현재 호가 기준 잔여 기대값이 음수 |
| `trade_flow_reversal_confirmed` | **체결 흐름이 매도 우위로 전환** (신규) |
| `orderbook_reversal_confirmed` | 호가 불균형 반전 |
| `spread_blowout` | 스프레드가 잔여 이익을 파괴 (신규) |
| `bid_support_evaporated` | 매수 지지 붕괴 (신규) |
| `live_risk_*` | 스푸핑·매도흡수·지지붕괴 |
| `trend_flipped_strong_down` | 4시간봉 강한 하락 전환 |

`TIME_EXIT` 은 SCALP 에서 **호출되지 않습니다.** `decideExit(..., allowTimeExit=false)`
로 경로 자체를 끕니다. TREND 전략은 기존 동작 그대로입니다.

시간이 남아 있는 곳은 단 하나, `decayedPWin()` 입니다. 보유가 길어질수록 **진입 당시
판독의 가중치를 낮춥니다.** 이건 청산 트리거가 아니라 신뢰도 모델이고, 시장 데이터
판정에 들어가는 추정치를 바꿀 뿐입니다.

## 체결 흐름 공급 (게이트웨이)

시간이 안전망에서 빠졌으니 시장 데이터 쪽 근거를 넓혀야 했습니다. 그런데 모니터가
받던 `quote` 에는 **호가만 있고 체결이 없었습니다.** 호가는 의도를 말하고 체결은
실제로 벌어진 일을 말하는데, 후자를 못 보고 있었습니다.

- 업비트 `/v1/trades/ticks`, 바이낸스 `/api/v3/trades` — 둘 다 공개 API
- 반감기 20초 가중으로 `trade_flow.pressure` 산출 (−1 = 순수 매도 공격, +1 = 매수 공격)
- 조회 실패는 `catch` 로 흡수. **체결 조회가 청산 경로를 깨뜨릴 수 없습니다**

업비트는 `ask_bid === "BID"` 가 매수자가 호가를 걷어간 것, 바이낸스는
`isBuyerMaker === false` 가 같은 의미입니다.

## 데이터 정체는 경보이지 매도가 아닙니다

기존 안전 TTL 을 없애면서, 그 자리를 **강제 청산이 아닌 감지**로 바꿨습니다.

```
marketDataStale(경과분) → POSITION_UNEVALUATED (CRITICAL) 이벤트 + 플래그
```

호가가 계속 들어오는 포지션은 **아무리 오래돼도 시간 때문에 닫히지 않습니다.**
호가가 끊긴 포지션은 애초에 관리되고 있지 않은 것이고, 그건 운영자 판단 사안이지
보이지도 않는 시장에 대고 추측으로 파는 근거가 아닙니다. 기본 30분, 조정 가능합니다.

## 신규 설정 (`202607260010_remove_time_exit.sql`)

| 컬럼 | 기본값 | 의미 |
|---|---|---|
| `scalp_hold_reversal_trade_pressure` | −0.30 | 이 이하면 매도 공격으로 판정 (2회 연속 시 청산) |
| `scalp_hold_max_spread_bps` | 40 | 스프레드 폭발 기준 |
| `scalp_hold_min_bid_depth_ratio` | 0.35 | 진입 시점 대비 매수 지지 잔존 비율 |
| `scalp_stale_data_minutes` | 30 | 데이터 정체 경보. **매도 트리거 아님** |

기존 열린 포지션의 `max_holding_at` 은 30일 뒤로 밀어둡니다. SCALP 는 이 값을 읽지
않지만, 운용 중 전략을 TREND 로 되돌릴 경우 낡은 값이 살아있는 포지션을 닫는 일이
없도록 하기 위함입니다.

대시보드 라벨도 `시간 기반 청산 없음 · 도달예상 ≤N분` 으로 변경했습니다.

## 검증

CI 동일 명령, 타입검사 포함 **174개 + 마이그레이션 가드 3 + 버전 가드 2 통과 / 0 실패.**

새 테스트 중 핵심:

- `elapsed time alone never closes a position` — 보유 5분·45분·120분·600분·5000분
  전부 `HOLD` 인지 확인
- `executed flow turning against the position closes it`
- `a liquidity event closes the position`
- `missing market data is an alert, not a sell`

---

# v5.7.3 — 버전 표기 수정 · 보유시간 라벨 정정

## 1. 상단 배너 버전이 v5.2.3 에 멈춰 있었음

배너는 `market-scanner/engine.ts` 의 `ENGINE_VERSION` 을 읽는데, 이 상수가
**v5.2.3 이후 한 번도 갱신되지 않았습니다.** 이후 모든 릴리스에서
`market-autotrader` 와 게이트웨이 버전만 올리고 스캐너를 빠뜨렸습니다.

아무것도 실패하지 않으니 드러나지 않았지만, **배포가 중간에 실패해 함수가
교체되지 않았을 때 어느 빌드가 돌고 있는지 확인할 방법이 없다**는 뜻이었습니다.
실제로 지난 배포에서 그런 상황이 있었습니다.

- `ENGINE_VERSION` 5.2.3 → **5.7.3**
- `docs/index.html` 배너 폴백 v5.2.0 → **v5.7.3**
- `docs/config.js` `uiVersion` 5.3.0 → **5.7.3**
- 운영 패널의 `v${data.version}` 은 **설정 리비전 번호**(trading_settings.version)이지
  앱 버전이 아니어서, `설정 rev.N` 으로 표기를 바로잡음

**CI 가드 신설** (`version.test.ts`) — 스캐너·자동매매·게이트웨이 세 상수가
일치하는지, `index.html` 배너가 엔진보다 낡지 않았는지 검사합니다.

## 2. "최대 45분 보유" 는 사실이 아니었음

라벨이 `scalp_max_holding_minutes` 를 "최대 N분 보유" 로 표기했는데,
**이 필드는 v5.4 이후 강제청산 시각이 아닙니다.**

| 설정 | 실제 의미 | 기본값 |
|---|---|---|
| `scalp_max_holding_minutes` | 변동성으로 산출하는 **목표 도달 예상시간의 상한**. 배리어 산정과 `expected_resolution_at` 체크포인트에 쓰임. 이 시각이 지나면 보유 판단 기준이 엄격해질 뿐 청산되지 않음 | 45분 |
| `scalp_safety_ttl_minutes` | **실제로 시간 때문에 청산되는 유일한 시각**. 장부 고착·모니터 장애 대비 백스톱 | 120분 |

`max_holding_at` 컬럼에 들어가는 값은 후자입니다. 라벨을
`도달예상 ≤45분 · 안전청산 120분` 으로 바꿔 둘을 분리 표기합니다.

### 두 숫자 모두 운영자 설정입니다

45분과 120분은 v5.6 에서 제가 기본값으로 넣은 것이고, 승인받은 값이 아닙니다.
대시보드 또는 아래 환경변수로 조정하십시오.

```
SCALP_MAX_HOLDING_MINUTES   도달 예상 상한 (1~1440)
SCALP_SAFETY_TTL_MINUTES    안전청산 (10~10080)
```

도달 예상 상한을 늘리면 저변동 종목도 배리어에 도달 가능한 것으로 평가되어
후보가 늘어나고, 줄이면 빠른 종목만 남습니다. 안전청산은 순수한 고장 방지용이라
정상 매매에는 관여하지 않습니다.

**검증:** 기존 173개 + 마이그레이션 가드 3개 + 버전 가드 2개 통과 / 0 실패.
스키마 변경 없음 — 마이그레이션 추가 없습니다.

---

# v5.7.1 — 마이그레이션 배포 실패 수정

## 긴급 — `column "scalp_resting_tp" does not exist` (SQLSTATE 42703)

`202607260008_maker_execution.sql` 5번 문장에서 배포가 중단됐습니다.

**원인:** `scalp_resting_tp` 는 v5.3 에서 **환경변수·기본값 설정으로만 추가하고 DB 컬럼을
만든 적이 없습니다.** 코드가 읽기만 할 때는 값이 없으면 기본값으로 폴백하므로 아무 문제가
없었고, v5.5 마이그레이션이 처음으로 이 컬럼에 **쓰기를 시도한 순간** 드러났습니다.
타입검사와 테스트를 모두 통과한 뒤 마이그레이션 단계에서 터진 이유입니다.

같은 누락이 2개 더 있었습니다.

| 컬럼 | 상태 | 영향 |
|---|---|---|
| `scalp_resting_tp` | 컬럼 없음 | **배포 실패** (SQL 이 쓰기 시도) |
| `scalp_minimum_edge` | 컬럼 없음 | 조용히 기본값 폴백, 대시보드에서 변경 불가 |
| `scalp_maker_slippage_allowance` | 컬럼 없음 | 동일 |

**조치**

1. 마이그레이션 008 에 세 컬럼을 **사용 전에** 생성하도록 추가
2. 008·009 의 제약 추가를 `drop constraint if exists` 선행으로 변경.
   마이그레이션이 중간에 실패하면 제약이 남을 수 있어, 재푸시가 "이미 존재함" 으로
   다시 깨지는 것을 막습니다. 004·006 도 같은 형태로 통일했습니다
   (Supabase 는 적용 이력을 버전으로 추적하므로 라이브 DB 에는 영향 없음)
3. **CI 가드 신설** — `migrations.test.ts`

### CI 가 이 유형을 앞으로 잡습니다

이번 건의 진짜 문제는 오타가 아니라, **타입검사·테스트를 다 통과해도 스키마 불일치는
아무도 안 보고 있었다**는 점입니다. 리포지토리를 직접 읽어 검사하는 테스트를 추가했습니다.

- SQL 에서 쓰거나 제약을 거는 모든 `scalp_*` 컬럼이 **그 이전 마이그레이션에서 생성되는지**
- 엔진이 읽는 모든 `scalp_*` 설정이 **컬럼으로 존재하는지** (없으면 대시보드에서 못 바꿈)
- 모든 `add constraint` 앞에 `drop constraint if exists` 가 있는지

`deno.json` 의 test 태스크에 `--allow-read` 를 추가했습니다.

**검증:** 기존 173개 + 마이그레이션 가드 3개 통과 / 0 실패.

---

# v5.7.0 — pWin 모델 오류 수정 (배리어 미반영)

## 이게 지금까지의 모든 판단을 왜곡하고 있었습니다

지적받은 대로 확인했습니다. `evaluateScalpSignal()` 에서 `targetPct` / `stopPct` 는
**기대값 계산에만 쓰이고 pWin 자체에는 전혀 들어가지 않았습니다.**

```ts
// v5.6 이전
let pWin = cfg.basePWin           // 0.52 고정
  + imbalanceTerm + pressureContribution + stabilityContribution
  + breakoutContribution + bidWallContribution - absorptionPenalty + dynamicAdjustment;
```

목표가 0.6%든 4%든 **동일한 확률**을 씁니다. 목표가 멀어지면 도달확률이 떨어져야 하는데
그게 반영되지 않아, 배리어를 넓힐수록 기대값이 공짜로 늘어나는 것처럼 보였습니다.

동일한 호가 신호에 손익비만 바꿔본 결과입니다 (총폭 1.5% 고정, 메이커 비용 0.07%):

| 손익비 | 중립승률 | v5.6 pWin | v5.7 pWin | v5.6 EV | v5.7 EV | 부풀림 |
|---|---|---|---|---|---|---|
| 1:1 | 50.0% | 67.4% | 65.4% | 0.191% | 0.161% | +0.030%p |
| 2:1 | 33.3% | **67.4%** | 48.7% | 0.441% | 0.161% | **+0.280%p** |
| 4:1 | 20.0% | **67.4%** | 35.4% | 0.641% | 0.161% | **+0.480%p** |
| 8:1 | 11.1% | **67.4%** | 30.0% | 0.774% | 0.213% | **+0.561%p** |

**설계가 계속 목표폭을 넓히는 쪽으로 흘러간 이유가 이것입니다. 모델이 그렇게 하면
보상을 줬습니다.** v5.4 의 "목표 2~4%" 도 이 왜곡 위에서 나온 결론이었습니다.

## 수정

pWin 은 절대확률이 아니라 **배리어 기준선 위의 엣지**로 재정의했습니다.

```
중립승률 = S / (T + S)          (무편향 배리어 도달확률)
pWin    = 중립승률 + 신호엣지
```

- `basePWin` 0.52 고정 기저 제거. `neutralWinRate` 를 geometry 가 공급
- 미공급 시에도 `stopPct / (targetPct + stopPct)` 로 역산 — 0.52 로 폴백하지 않음
- 추세 페널티는 **확률이 아니라 엣지에** 적용. 약한 하락이 기하학적 기준선 아래로
  확률을 끌어내릴 수는 없음
- 주문 시점 재계산(`refreshPWinAtOrderTime`)과 보유 판단(`decayedPWin`)도 동일하게
  **중립승률로 감쇠**. 신호가 낡으면 평평한 상수가 아니라 그 포지션의 배리어가
  함의하는 값으로 돌아감
- 후보 스냅샷에 `neutral_win_rate` / `signal_edge` 노출 — 캘리브레이션이 절대확률이
  아니라 엣지를 직접 학습할 수 있게 됨

`edgeBudget` 이 이제 모델 전체에서 같은 뜻을 갖습니다. 배리어 산정에 쓰는 δ 와
pWin 이 주장하는 엣지가 동일한 양입니다.

## 검증

CI 동일 명령, 타입검사 포함 **173개 통과 / 0 실패**.

기존 `signal.test.ts` 2건이 BUY → WAIT 로 바뀌어 실패했고, 이는 **수정의 직접 결과**입니다.
부풀려진 pWin 덕에 통과하던 케이스였습니다. 2:1 배리어는 중립승률 상한이 33% 이므로
같은 호가 신호로 기대값 문턱을 넘지 못하는 것이 정상입니다. 테스트를 실제 운용 기하구조
(약 1:1, 중립 48%)로 갱신하고, 회귀 방지 테스트 3건을 추가했습니다.

## 통화 표기

손익은 통화를 섞어 환산하지 않습니다. 9건 합계는 **바이낸스 −4.3050 USDT / 업비트
−4,520원**, 승률 1/9 = 11.1% 입니다.

---

# v5.6.0 — 처리량 최적 스캘핑

## 정정: v5.4의 "목표 2~4% · 보유 6시간"은 폐기합니다

지적이 맞습니다. 그건 비용 문제를 푼 게 아니라 **스캘핑을 스윙으로 바꾼 것**입니다.
제가 목적함수를 잘못 잡았습니다. 최대화해야 할 것은 거래당 기대값이 아니라
**단위시간당 기대값**입니다.

```
거래당 기대값 = δ·W − C          (W = 목표+손절 폭, δ = 초과승률, C = 왕복비용)
배리어 도달시간 ∝ W²             (무작위 보행: 거리²에 비례)
시간당 수익률  ∝ (δ·W − C)/W² = δ/W − C/W²

d/dW = 0  ⇒  W* = 2C/δ,   최대 시간당수익 ∝ δ²/(4C)
```

**최적 폭은 하한이 아니라 점(point)입니다.** 이 지점을 넘겨 넓히면 거래당 이익은
늘지만 회전이 그보다 더 느려져 총이익이 줄어듭니다. v5.5까지의 "손익분기+마진" 방식은
상한이 없어서 계속 스윙 쪽으로 흘렀습니다.

| 비용 C | δ=0.10 | δ=0.15 | δ=0.20 |
|---|---|---|---|
| 테이커 0.17% | 3.40% | 2.27% | 1.70% |
| **메이커 0.07%** | **1.40%** | **0.93%** | **0.70%** |

제가 2~4%를 제안한 건 테이커 비용을 고정 전제로 뒀기 때문입니다.
메이커 비용에서 최적 폭은 0.7~1.4% — **정확히 스캘핑 영역**입니다.

## 승률 50% 이상은 취향이 아니라 산수입니다

배리어 쌍의 중립 승률은 정확히 `S/(T+S)` 입니다. **손익비 2:1 은 어떤 신호를 써도
승률 상한이 33%** 입니다. 50% 이상을 원하면 손절폭이 목표폭보다 좁아선 안 됩니다.

`scalp_target_win_rate` 를 신설해 손익비를 **역산**합니다.

| 목표승률 | 손익비 | 목표 | 손절 | 중립승률 | 실현승률 | 거래당 EV |
|---|---|---|---|---|---|---|
| 55% | 1.22 | 0.88% | 0.72% | 45.0% | 55.0% | +0.080% |
| **58% (기본)** | **1.08** | **0.83%** | **0.77%** | 48.0% | **58.0%** | **+0.080%** |
| 60% | 1.00 | 0.80% | 0.80% | 50.0% | 60.0% | +0.080% |

최적점에서는 거래당 기대값이 정확히 왕복비용과 같아집니다(EV = C). 보유시간은 45분
이내로 환원했고, 안전 TTL 도 6시간 → 2시간으로 내렸습니다.

## 전제 조건: 유니버스를 거래대금이 아니라 변동성으로 고른다

이게 실거래 9건이 실패한 진짜 이유입니다. 스캐너가 **24시간 거래대금 상위 40개**로
유니버스를 잘랐고, 그건 곧 대형주 = 단기 변동폭이 비용 대비 가장 작은 종목입니다.
KRW-ETH 에 8시간 동안 4번 들어간 이유가 이것입니다. ETH 의 30분 변동은 0.04~0.11%,
업비트 왕복비용은 0.10%. **방향과 무관하게 이길 수 없었습니다.**

`SCALP_UNIVERSE_CAP` 정렬 기준을 24시간 거래대금 → **24시간 변동폭(`day_range_pct`)**
으로 변경했습니다. 유동성은 하드 하한으로 유지하고(스프레드가 넓어지면 메이커 경제성이
무너지므로), 그 위에서는 움직이는 종목을 우선합니다. **거래 기회를 줄이는 게 아니라
늘리는 변경입니다** — 변동성이 큰 종목일수록 배리어에 빨리 도달하므로 회전이 빨라집니다.

### 필요 변동성 (업비트 메이커, 목표승률 58%)

| edgeBudget | 목표 | 손절 | 20분 보유 | 30분 보유 | 45분 보유 |
|---|---|---|---|---|---|
| 0.10 | 0.83% | 0.77% | ATR 0.67% | 0.54% | 0.44% |
| 0.13 | 0.68% | 0.55% | 0.54% | 0.44% | 0.36% |
| 0.15 | 0.61% | 0.46% | 0.49% | 0.40% | 0.32% |
| 0.20 | 0.50% | 0.30% | 0.40% | 0.32% | 0.26% |

실거래 종목의 5분봉 ATR: ETH 0.06% / LINK 0.05% / EPIC 0.14% / AVAX 0.20% / DOGE 0.27%.
**전부 스캘핑 가능 하한 미만이었습니다.**

## 신규 설정 (`202607260009_throughput_scalping.sql`)

| 컬럼 | 기본값 | 의미 |
|---|---|---|
| `scalp_target_win_rate` | 0.58 | 봇이 실제로 찍어야 할 승률. 손익비를 역산 |
| `scalp_reward_risk` 하한 | 1.0 → **0.4** | 목표가 손절보다 좁은 고승률 프로파일 허용 |
| `scalp_max_holding_minutes` | 120 → **45** | 스캘핑 지평 환원 |
| `scalp_safety_ttl_minutes` | 360 → **120** | 백스톱 축소 |

환경변수 `SCALP_LIQUIDITY_FLOOR_RATIO` 로 유동성 하한 배수 조정 가능.

## 검증

CI 동일 명령, 타입검사 포함 **170개 통과 / 0 실패** (throughput 11개 신규).

---

# v5.5.0 — 메이커 체결 전환

## 방향 전환: 거래를 줄이는 게 아니라, 거래당 비용을 뒤집는다

v5.4까지는 진입·청산 **양쪽 모두 테이커**였습니다.

- 진입: 지정가 IOC를 최우선 **매도호가**에 → 스프레드 지불
- 청산: **시장가** → 최우선 매수호가에 → 스프레드 또 지불

즉 왕복 실비용이 `수수료 + 스프레드`였습니다. 2026-07-26 실거래 9건의
|가격변동| 중앙값이 **0.109%**, 업비트 왕복 실비용이 **0.170%**입니다.
**이길 수 없는 구조였습니다.** LINK는 +0.036% 맞추고 -0.19%, EPIC은 +0.220% 맞추고 +0.01%.

메이커로 걸면 부호가 뒤집혀 `수수료 − 스프레드`가 됩니다. 업비트·바이낸스는
메이커·테이커 수수료가 같으므로 **이득 전부가 스프레드**입니다.

| | 테이커 | 메이커 | 개선 |
|---|---|---|---|
| 업비트 | 0.170% | **0.070%** | 2.4배 |
| 바이낸스 BNB | 0.220% | 0.120% | 1.8배 |

### 승률 자체도 오릅니다

이건 문턱만 낮추는 게 아닙니다. 스프레드만큼 낮게 사면 **시장 현재가 기준으로
목표는 그만큼 가까워지고 손절은 그만큼 멀어집니다.** 배리어 폭은 그대로인데
위치가 내려갑니다.

| 스프레드 | 테이커 중립승률 | 메이커 중립승률 | 이득 |
|---|---|---|---|
| 0.02% | 33.3% | 35.6% | +2.2pp |
| 0.05% | 33.3% | 38.9% | +5.6pp |
| 0.10% | 33.3% | 44.4% | +11.1pp |

청산까지 메이커(목표가 예약매도)면 같은 효과가 한 번 더 붙습니다.
목표 0.6%/손절 0.3%, 스프레드 0.05% 기준으로 **실현 승률 +11.1pp, 필요 승률 −11.1pp**
— 격차가 16.7pp 좁혀집니다.

## 구현

**게이트웨이**
- Binance `LIMIT_MAKER` 지원. 체결 시 거래소가 **주문을 거절**하므로 실수로 테이커가 되는 사고가 원천 차단됨
- Upbit은 post-only 플래그가 없어, 최우선 매수호가 이하 가격 + `time_in_force` 미지정으로 메이커 보장
- `LIMIT_MAKER` 를 `order_test` 에도 허용

**자동매매 엔진**
- `makerBidPrice()` — 매수호가를 절대 넘지 않게 틱 내림
- `syncMakerEntry()` — 미체결 대기 상태기계. TTL(기본 90초) 경과 또는 호가가 3틱 이상 상승하면 취소.
  부분체결은 거래소 최소주문을 넘을 때만 장부에 반영, 아니면 전량 취소
- **미체결은 손실이 아닙니다.** `MAKER_ENTRY_UNFILLED` 로 기록하고 학습 표본에서 제외
- 예약 익절(`scalp_resting_tp`) **기본 ON 으로 전환** — 메이커 청산 레그이므로 더 이상 선택사항이 아님
- 메이커일 때 슬리피지 가정 0.09% → **0.03%**. 호가를 넘지 않으므로 진입 슬리피지가 없고, 손절 분기만 시장가

**배리어**
- `spreadCaptureFraction` 신설. 비용 산정이 `수수료 + 슬리피지 − 스프레드회수` 로 변경
- 테이커 비용이 강요하던 2% 배리어가 필요 없어짐 → 보유시간 상한 60분으로 환원

## edgeBudget 다이얼 — 스캘핑 강도는 여기서 정합니다

업비트 메이커 기준으로 `scalp_edge_budget` 이 배리어를 결정합니다.

| edgeBudget | 최소마진 | 목표 | 손절 | 의미 |
|---|---|---|---|---|
| 0.100 (기본) | 0.10% | 1.20% | 0.60% | 보수적 |
| **0.133** | 0.05% | **0.65%** | **0.33%** | 원래 쓰시던 스캘핑 강도 |
| 0.150 | 0.05% | 0.58% | 0.29% | 공격적 |
| 0.200 | 0.05% | 0.43% | 0.22% | 매우 공격적 |

**참고로 테이커 시절의 0.6%/0.3% 는 암묵적으로 32pp 를 가정하고 있었습니다.**
메이커 전환으로 같은 배리어의 요구치가 13.3pp 로 내려온 것이고, 13.3pp 가 실제로
나오는지는 여전히 **미검증**입니다. 캘리브레이션 루프가 측정할 대상이 바로 이것입니다.

## 솔직한 리스크

메이커는 **불리할 때 우선 체결됩니다.** 가격이 내려오면 매수호가가 먼저 채워지고,
올라가면 안 채워집니다. 이게 메이커의 진짜 적이고, 호가 불균형 신호가 원래
이걸 거르라고 있는 것입니다.

체결률이 낮으면 **거래 횟수가 오히려 줄 수 있습니다.** 다만 미체결은 비용이 0이라
손실 없이 재시도할 수 있고, `MAKER_ENTRY_RESTED` 대비 `MAKER_ENTRY_FILLED` 비율로
며칠 안에 측정됩니다. 체결률이 낮게 나오면 하이브리드(N초 후 테이커 전환)로
내려오면 되지만, 반대 순서로는 데이터가 안 나옵니다.

## 신규 설정 (`202607260008_maker_execution.sql`)

| 컬럼 | 기본값 | 의미 |
|---|---|---|
| `scalp_maker_entry` | true | 매수호가에 걸어 진입 |
| `scalp_maker_entry_ttl_seconds` | 90 | 미체결 대기 시간 |
| `scalp_maker_entry_drift_ticks` | 3 | 호가 이탈 시 조기 취소 |
| `scalp_spread_capture` | 0.0005 | 배리어 산정용 스프레드 회수 가정 |
| `scalp_maker_slippage_allowance` | 0.0003 | 메이커 슬리피지 가정 |
| `scalp_resting_tp` | **true** | 예약 익절 (기본 ON 전환) |

---

# v5.4.2 — 배포 검증 범위 수정

## 긴급 2 — 2026-07-26 배포 실패

**증상:** `Deploy Supabase Trading Engine #58` 실패.

```
TS2739 [ERROR]: Type '{ samples: ... }' is missing the following properties
from type 'Microstructure': book_imbalance_top, trade_pressure_fast
  at backtest/neutral-micro.ts:24:3
```

**원인:** v5.3에서 `Microstructure` 타입에 SCALP 전용 필드 2개를 추가했는데,
`backtest/neutral-micro.ts` 의 중립 스텁이 이 필드를 갖고 있지 않았습니다.
제가 로컬 검증을 `supabase/functions/` 경로로만 돌렸고, CI 의 `deno task check` 는
`backtest/run.ts` · `backtest/calibrate.ts` 까지 검사한다는 점을 놓쳤습니다. **제 실수입니다.**

**조치:**

1. `neutral-micro.ts` 에 `book_imbalance_top: 0`, `trade_pressure_fast: 0` 추가.
   백테스트는 과거 오더북이 없어 이 값을 재현할 수 없으므로, 기존
   `book_imbalance` / `trade_pressure` 와 동일한 이유로 중립값을 주입합니다
2. **`deno.json` 의 CI 검사 범위 확대** — 이게 더 중요합니다

### CI 가 제 테스트를 돌리지 않고 있었습니다

기존 태스크:

```
check: market-scanner, market-learning, market-autotrader, backtest/run, backtest/calibrate
test:  market-scanner/*.test.ts, market-autotrader/*.test.ts, backtest/*.test.ts
```

**`supabase/functions/_shared/scalp/*.test.ts` 가 없습니다.** v5.3~v5.4에서 추가한
geometry 13 · calibration 12 · hold 14 · base-fee 9 = **48개 테스트가 배포를 전혀
게이트하지 않고 있었습니다.** `scalp-calibration/index.ts` 도 타입검사 대상이 아니었습니다.

수정 후:

```
check: + scalp-calibration/index.ts, market-performance/index.ts
test:  + supabase/functions/_shared/scalp/*.test.ts
```

`deno test` 는 기본으로 타입검사를 하므로, 테스트 범위를 넓히면서 `signal.test.ts` 에
있던 선행 타입오류(TS2367 — `assert()` 의 `asserts` 시그니처가 유니온을 좁혀
다음 줄 비교를 거부)도 함께 고쳤습니다. 단언 순서만 바꿨고 검증 내용은 동일합니다.

**검증:** CI 와 동일한 명령으로 타입검사 포함 **159개 통과 / 0 실패**, 대상 파일 16개.

---

# v5.4.1 — Base-Asset Commission Fix (운영 장애 대응)

## 긴급 — 2026-07-26 운영 장애 원인과 조치

**증상:** `SAFETY_POSITION_MISMATCH:binance:AVAX` → 양 거래소 전 종목 신규진입 정지.
사용자는 수동 거래를 한 적이 없음.

**원인:** 바이낸스 현물은 BNB 수수료 결제가 꺼져 있으면 **매수 수수료를 받은 코인에서**
뗍니다. `executedQty` 는 수수료 차감 **전** 총 체결량인데, `applyEntryAccounting()` 이
이 값을 그대로 `initial_quantity` / `remaining_quantity` 에 넣었습니다. 따라서 장부는
계좌보다 항상 약 0.1% 많은 수량을 기대하게 됩니다.

계좌 대조는 이 기대치를 **0.0001% 허용오차**로 실제 잔고와 비교했고, "코인이 사라졌다"
= 수동 매도로 판정한 뒤 3회 확인(45초) 만에 `manual_intervention_required` 를 세웠습니다.
사용자 행위는 전혀 개입되지 않았습니다.

**검증:**

| 항목 | 값 |
|---|---|
| 투자금 / 진입가 | 261.4076 USDT / 6.781 |
| 장부 기록 수량 (총 체결량) | 38.55001 AVAX |
| 계좌 실제 수량 | 38.51275 AVAX |
| 차이 | 0.03726 AVAX = **0.2526 USDT** |
| 대시보드 보고 불일치 | **0.2528 USDT** |

업비트는 수수료를 원화로 떼므로 기초자산 수량이 영향을 받지 않습니다. 불일치가
바이낸스에서만 발생한 이유입니다.

**조치 (3중):**

1. `baseAssetFee()` 신설 — 체결 단위로 수수료 자산을 확인해, 수수료가 기초자산으로
   부과된 경우에만 차감. `applyEntryAccounting()` 이 **실제로 받은 순수량**을 장부에 기록
2. 계좌 대조에 **수수료 크기 허용오차** 도입 (수수료율 × 3, 또는 수량단위 × 2 중 큰 값).
   이 범위 안의 부족분은 정지가 아니라 **장부를 계좌에 맞춰 자동 정렬**(`LEDGER_FEE_DUST_HEALED`).
   v5.4.1 이전에 생성된 포지션과 수량단위 반올림 잔여도 이 경로로 치유됩니다
3. 마이그레이션 `202607260007` — `trading_fills` 에 기록된 실제 수수료로 기존 열린 포지션
   수량을 보정하고, 이 버그로 걸린 정지 플래그를 해제하고, 오탐으로 `MANUAL_RECONCILE`
   청산된 포지션을 학습 표본에서 제외

**참고:** 바이낸스에서 **"BNB로 수수료 지불"** 을 켜면 수수료가 BNB로 부과되어 기초자산
수량이 애초에 줄지 않습니다. 수수료 25% 할인까지 함께 얻으므로 권장합니다.

**중요:** 이 장애가 발생한 시점의 운영 버전은 **v5.2.5** 입니다. 로그의 청산시간이 정확히
30분 0초 / 30분 1초 / 30분 7초 / 30분 10초 / 30분 15초이고, v5.4는 30분 강제청산을
삭제했습니다. `SAFETY_PAUSE` 문구도 v5.2.5 원문입니다. 즉 **v5.4는 아직 배포되지
않았습니다.** 그리고 v5.4를 배포했더라도 이 건은 막히지 않았을 것입니다 — v5.4가 고친 것은
"봇 자신의 미체결 주문이 잔고를 **잠그는**" 경우이고, 이번 건은 수수료로 수량이 실제로
**줄어든** 경우라 다른 경로입니다.

---

# v5.4.0 — Live-Edge Holding, Asset-Scoped Locks, Real Fees

(v5.3 내용 포함. 아래 0절에 이전 리뷰에서 **제가 틀렸던 부분의 정정**을 먼저 정리했습니다.)

## 0. 정정 — 앞선 리뷰에서 제가 틀렸던 것

| 제가 한 말 | 실제 | 반영 |
|---|---|---|
| "배리어를 넓히면 승률 자체가 오른다" | **틀렸습니다.** 목표·손절을 같은 비율로 넓히면 중립 승률은 `S/(T+S)`로 계속 33.3%입니다. 달라지는 건 비용이 목표폭에서 차지하는 비율, 즉 **필요 승률**입니다 | 코드는 처음부터 맞게 짜여 있었습니다(`neutralWinRate`는 항상 33.3%). 채팅 요약 표현이 부정확했습니다 |
| "BNB 25개 이상 보유하면 0.075%" | **틀렸습니다.** 25% 할인은 **"BNB로 수수료 지불" 활성화** 조건이지 특정 수량 보유 조건이 아닙니다. VIP 등급은 별개입니다 | 문서에서 삭제. 더 나아가 **계정 실제 수수료를 조회**하도록 변경(3절) |
| "지정가 익절은 난이도 낮음" | **틀렸습니다.** 계좌 대조가 봇 자신의 잠긴 잔고를 수동 개입으로 오인합니다 | v5.4에서 대조 로직을 먼저 고쳤습니다(2절). 난이도는 중간이 맞습니다 |
| "시스템 전체가 시간당 최대 2회" | 부정확. 거래소당 2회, 시스템 전체로는 약 4회입니다 | 표현 정정 |
| 슬롯 6 / 목표 2%·손절 1% | 6은 **기본값**이지 하드코딩이 아니며 `scalp_position_slots`로 조정 가능합니다. 2%/1%는 애초에 고정하지 않았고 변동성 스케일로 구현했습니다 | v5.4에서 대시보드 범위 제약을 명시(`max_new_entries_per_scan` 1~4 등) |

---


목표: **승률 향상**. 게이트를 조여 거래 횟수를 줄이는 방식은 사용하지 않음.
빈도는 동시 포지션 수로 확보하고, 승률은 배리어 기하구조·신호 신선도·청산 방식으로 확보.

---

## 1. 왜 v5.2.5에서는 승률을 올릴 수 없었는가

배리어 (목표 T, 손절 S)에서 알파가 없을 때 목표를 먼저 찍을 확률은 `S / (T + S)`.

- v5.2.5: T=0.60%, S=0.30% → **중립 승률 33.3%**
- 왕복비용 C + 최소마진 m 을 넘으려면 `p ≥ (C + m + S) / (T + S)`

| 거래소 | C(수수료+슬리피지) | 필요 pWin | 중립 대비 필요 초과승률 |
|---|---|---|---|
| 업비트 | 0.19% | 65.6% | **32.2pp** |
| 바이낸스 | 0.29% | 76.7% | **43.3pp** |

`signal.ts` 의 pWin 은 현실적 최댓값이 약 0.67. 즉 **업비트는 거의 모든 지표가 동시에
최상일 때만 통과, 바이낸스는 구조적으로 통과 불가**. 거래가 0에 수렴한 것은 로직이
엄격해서가 아니라 배리어가 비용 대비 너무 좁았기 때문.

핵심 항등식:

```
필요 초과승률 δ = (C + m) / (T + S)
```

알파를 만들어내지 않고 δ 를 낮추는 유일한 방법은 **T + S 를 넓히는 것**.

---

## 2. 변경 사항

### 2-1. `_shared/scalp/geometry.ts` (신규) — 종목별 배리어·보유시간

고정 0.60%/0.30% 폐기. 종목의 5분봉 ATR 과 거래소 비용 구조에서 매번 산출.

```
필요폭   T + S = (C + m) / edgeBudget
비용바닥 T     = 필요폭 × rr / (1 + rr)
변동성   T     = targetSigmaMult × σ_5m × √(baseHorizon / 5)
최종     T     = clamp(max(비용바닥, 변동성), minTarget, maxTarget),  S = T / rr
보유시간       = clamp((T / (targetSigmaMult × σ_5m))² × 5분, min, max)
```

`edgeBudget`(기본 0.10) 이 이 시스템에서 가장 중요한 손잡이. **"신호가 실제로 벌어주는
초과승률"** 을 뜻하며, 낮출수록 배리어가 넓어지고 필요 승률이 내려감.

기본 설정에서의 산출 결과 (보유시간 상한 120분):

| 거래소 | ATR(5m) | 목표 | 손절 | 보유 | 결정요인 | 필요 초과승률 | 해결확률 |
|---|---|---|---|---|---|---|---|
| 업비트 | 0.20% | 1.93% | 0.97% | 120분 | COST_FLOOR | 10.0pp | 22% (차단) |
| 업비트 | 0.45% | 1.93% | 0.97% | 120분 | COST_FLOOR | 10.0pp | 72% |
| 업비트 | 1.00% | 2.76% | 1.38% | 120분 | VOLATILITY | 7.0pp | 95% |
| 업비트 | 2.00% | 4.00% | 2.00% | 63분 | MAX_TARGET | 4.8pp | 95% |
| 바이낸스 | 0.45% | 2.60% | 1.30% | 120분 | COST_FLOOR | 10.0pp | 50% |
| 바이낸스 | 1.00% | 2.76% | 1.38% | 120분 | VOLATILITY | 9.4pp | 95% |

→ **필요 초과승률 32.2pp → 4.8~10.0pp.** 실현 가능한 범위로 내려옴.

### 2-2. 기대값 식에 시간청산 분기 추가 (`cost-model.ts`)

v5.2.5 는 `EV = pWin·T − (1−pWin)·S − 비용` 으로, **모든 거래가 배리어에서 끝난다고 가정**.
실제로는 최대 보유시간 도달이라는 세 번째 결과가 있고, 수익은 0에 가깝지만 왕복 수수료는
그대로 나감. 저변동 종목에서는 이쪽이 다수 결과.

```
pTarget  = pWin       × resolveProbability
pStop    = (1 − pWin) × resolveProbability
pTimeout = 1 − resolveProbability
```

`resolveProbability` 는 geometry 가 종목별로 산출. 위 표에서 저변동 메이저가 차단되는 것은
게이트를 새로 건 게 아니라 **기대값이 처음으로 정확히 계산됐기 때문**.
`resolveProbability` 미지정 시 1 → v5.2.5 와 수치적으로 완전 동일 (하위호환).

### 2-3. 주문 시점 신호 재계산 (`signal.ts` / `market-autotrader`)

v5.2.5 는 스캔 때 계산한 `pWin` 을 주문 시점에 **그대로 재사용**했고 호가 깊이만 갱신.
스캔 주기가 60초이므로 진입을 결정한 호가 판독이 1분 가까이 묵은 것일 수 있었음
(`maxBookAgeMs: 5000` 은 스캔 단계에서만 검사돼 신선도가 보장되는 듯한 착시를 줌).

`refreshPWinAtOrderTime()` 추가:
- 호가 항(`imbalanceContribution`)은 **주문 직전 실시간 호가로 재계산**
- 재관측 불가능한 항(체결압력·돌파·흡수)은 반감기 `alphaHalfLifeMs`(기본 20초)로 감쇠

### 2-4. 호가 신호 자체의 신선도 (`market-scanner/engine.ts`)

기존 `book_imbalance` / `trade_pressure` 는 TREND 및 백테스트 하니스가 쓰므로 **의미 불변**.
SCALP 전용 필드 2개 신설:

- `book_imbalance_top` — 전 호가 1/n^0.72 가중 + 관측창 전체 평균 → **상위 5호가, 1/n^1.5 감쇠, 최근 4프레임만**
  (깊은 호가는 스푸핑에 가장 취약하고 단기 예측력이 가장 낮음)
- `trade_pressure_fast` — 10분 단순 누적 → **반감기 45초 지수가중**

### 2-5. 자본 N분할 (`market-autotrader`)

v5.2.5:

```ts
const notionalQuote = floorToStep(Math.min(managedAvailable, maxOrder), limits.quoteStep);
```

첫 후보가 **거래소 가용 잔고 전액**을 사용 → 두 번째 후보는 항상 거래소 최소주문 미달로
탈락 → `max_new_entries_per_scan = 2` 는 코드상 무의미했고, 실제로는 **동시 1포지션**.

`scalp_position_slots`(기본 6) 신설. 1 로 두면 v5.2.5 동작 그대로.
시간당 이론 회전수 2회 → 10~16회. **거래 횟수가 늘어나는 변경.**

### 2-6. 후보 정렬을 스캘핑 기대값 기준으로

`loadBuyCandidates` 는 `order=score.desc` — **레거시 추세점수** 순.
SCALP 에서는 진입 판단이 호가 신호인데 자본을 받는 순서는 추세점수가 정하고 있었음.
`snapshot.scalp.provisional_edge` 내림차순으로 재정렬.

### 2-7. 1차 익절 후 본전 이동

v5.2.5:
- `exit_policy` 기본값 `SCALE_OUT` → 추적손절이 아예 붙지 않음
- 잔여 40% 가 **원래 손절 −0.30% 를 그대로 유지** → 목표를 찍고 되돌아오면 순손실
- `TRAIL_AFTER_T1` 이어도 1.2% 고정 거리라 `nextTrailingStop` 이 하드손절에서 floor 되어
  peak 가 +0.91% 를 넘기 전까지 **추적손절이 작동하지 않음** (목표는 +0.60%)

변경:
- SCALP 는 `exit_policy` 를 항상 `TRAIL_AFTER_T1` 로 강제
- 추적 거리 = **손절폭 1개분** (고정 1.2% 폐기)
- T1 체결 시 트레일을 **진입가 + 왕복수수료** 아래로 내려가지 않게 고정

※ `stop_price` 가 아니라 `trailing_stop` 에 적용. `trading_positions` 에
`check (stop_price < average_entry_price)` 제약이 있어 stop_price 로는 본전을 표현할 수 없음.
`decideExit()` 는 t1 완료 후 `max(stop_price, trailing_stop)` 을 쓰므로 스키마 변경 없이 동일 효과.

### 2-8. 진입 상한을 실시간 호가 기준으로

`candidate.entry_high` 는 15분봉 ATR 구조에서 나온 **추세 계획의 진입 구간 상단**이라
현재 매도호가보다 낮은 경우가 많았고, 그때마다 스캘핑 진입이 조용히 차단됐음.
SCALP 에서는 `bestAsk × (1 + min(0.25 × stopPct, 0.002))` 로 대체.

### 2-9. 지정가 예약매도 (`scalp_resting_tp`, 기본 OFF)

v5.2.5 익절 경로: **15초마다 티커 폴링 → 목표 이상이면 시장가 매도.** 같은 거래에서 양쪽으로 손해:

1. 15초 사이에 목표를 찍고 되돌아오면 **승리 자체가 발생하지 않음.** 포지션은 열린 채로
   남고 상당수는 나중에 손절로 끝남. 실현 승률에 직접적이고 큰 타격.
2. 폴링이 잡아내도 시장가라 최우선 **매수호가**에 체결. 즉 목표 − 스프레드.
   모든 승리가 EV 모델의 가정보다 작아짐.

진입 체결 즉시 목표가에 지정가 매도를 걸어두면 둘 다 사라짐. 거래소가 정확히 목표가에
체결시키고, 수익 경로에서 폴링이 완전히 빠짐.

상태기계 불변식:

- 예약주문은 **종료 상태에서 정확히 한 번만** 정산. OPEN/PARTIALLY_FILLED 동안은
  아무것도 기록하지 않으므로 부분체결 이중계상이 불가능
- 다른 청산(손절·시간·비상)은 **반드시 취소 확인 후** 시장가 매도.
  예약주문이 기초자산을 잠그므로, 취소 없이 매도하면 잔고 부족으로 거절되고
  포지션이 보호 없이 열린 채 묶임
- 취소 과정에서 체결이 확인되면 그 체결을 TARGET_1 으로 기록하고, 청산은
  강행하지 않고 다음 사이클에 재판단
- 취소가 3회 시도 후에도 확인되지 않으면 `TP_CANCEL_UNRESOLVED`(CRITICAL) 기록 후
  청산을 한 사이클 보류. **잔고 부족 매도를 보내지 않음**
- T1 슬라이스와 잔여 물량이 **둘 다** 거래소 최소주문을 넘을 때만 등록.
  아니면 기존 폴링 청산으로 폴백 (잔여가 매도 불가 dust 가 되는 것 방지)
- Upbit 은 지정가가 기본으로 잔류하고 `time_in_force` 는 ioc/fok 만 받으므로 필드 생략.
  Binance 는 게이트웨이가 미지정 시 IOC 를 넣으므로 `GTC` 명시

**기본값 OFF.** 주문 생애주기를 바꾸는 변경이므로 소액 실거래 확인 후 켜실 것.

### 2-10. pWin 캘리브레이션 루프 (`_shared/scalp/calibration.ts` + `scalp-calibration` 함수)

`signal.ts` 의 pWin 은 기저 0.52 에 0.18 / 0.12 / 0.08 / 0.10 가중치를 더하는 식.
**이 숫자들은 데이터에서 나온 게 아님.** 기존 포워드 러닝은 도움이 되지 않음 —
학습 파라미터가 scoreThreshold / minNetRR / shortTargetAtrMult / stopAtrMult 로 전부
추세 파라미터이고, 후보 쿼리도 `period_score` · `net_rr` 로 거르는데 SCALP 는 둘 다
게이트로 쓰지 않음. **읽히지 않는 프로필을 승격시키고 있었음.**

Platt 스케일링 도입:

```
p_calibrated = sigmoid(a × logit(p_raw) + b)      # a=1, b=0 이 항등(무보정)
```

- 특징 1개·파라미터 2개로 의도적으로 제한. 원 특징 전체에 대한 회귀는 이 시스템이
  현실적으로 만들 표본 수에서 심하게 과적합됨
- **수축**: 적합 결과를 `n / (n + 300)` 가중으로 항등 쪽으로 당김.
  표본이 쌓일수록 영향력을 획득
- **승격**: 시간순 분할(과거 → 학습, 최근 → 검증). 검증 Brier 가 기존 프로필보다
  0.002 이상 개선될 때만 승격. 무작위 분할이 아닌 이유는 미래 정보로 프로필이
  승격되는 것을 막기 위함
- 최소 200표본, 승패가 한쪽뿐이면 거부, 기울기 0.25~2.5 / 절편 ±1.5 로 클램프

승패 정의:

| 구분 | 조건 | 용도 |
|---|---|---|
| 승 | `t1_completed = true` | pWin 표본 (최종 청산 사유와 무관 — T1 을 먹고 트레일로 나갔어도 목표 도달) |
| 패 | 목표 미도달 + 손절/트레일 종료 | pWin 표본 |
| 미해결 | 시간청산·비상청산·수동정리 | pWin 표본에서 **제외**. pWin 은 배리어 해결 조건부 확률이므로. 대신 `realized_resolve_rate` 로 집계되어 geometry 의 `resolveProbability` 근사치를 대체할 근거가 됨 |

- 진입 시점의 특징 벡터·예측 pWin·기하구조를 `trading_positions.metadata.scalp_signal` 에 적재
- 주문 시점에 활성 프로필을 적용 (읽기 실패 시 항등으로 폴백 — 캘리브레이션 장애가
  매매 동작을 바꾸지 않음)
- 신뢰도 다이어그램(`reliability_bins`)을 프로필에 저장 — pWin 이 의미가 있는지에 대한
  정직한 그림

## 3. 신규 설정 (`202607260004_scalp_geometry_v53.sql`)

| 컬럼 | 기본값 | 의미 |
|---|---|---|
| `scalp_position_slots` | 6 | 자본 분할 수 = 동시 포지션 수. 1이면 v5.2.5 동작 |
| `scalp_edge_budget` | 0.10 | 신호가 벌어준다고 가정하는 초과승률. **낮출수록 배리어가 넓어짐** |
| `scalp_reward_risk` | 2.0 | 목표:손절 비율 |
| `scalp_slippage_allowance` | 0.0009 | 배리어 산정용 양방향 슬리피지 가정 (실제 슬리피지는 주문 시점에 호가로 시뮬레이션) |
| `scalp_alpha_half_life_ms` | 20000 | 주문 시점 재계산 시 스캔 알파 감쇠 반감기 |
| `scalp_breakeven_after_t1` | true | T1 후 본전 이동 |
| `scalp_resting_tp` | **false** | 목표가 지정가 예약매도. 소액 확인 후 켤 것 |
| `scalp_max_holding_minutes` | 30 → **120** | 보유시간 상한 (종목별 실제 보유시간은 geometry 가 산출) |

동일 이름의 환경변수로도 지정 가능
(`SCALP_POSITION_SLOTS`, `SCALP_EDGE_BUDGET`, `SCALP_REWARD_RISK`, `SCALP_SLIPPAGE_ALLOWANCE`,
`SCALP_ALPHA_HALF_LIFE_MS`, `SCALP_BREAKEVEN_AFTER_T1`, `SCALP_MAX_HOLDING_MINUTES`,
`SCALP_RESTING_TP`).

신규 테이블: `scalp_calibration_profiles` (`202607260005_scalp_calibration.sql`).
항등 프로필이 시드로 들어가므로 승격 전까지 동작은 v5.2.5 와 동일.

---

## 4. 검증

- 전체 테스트 **127개 통과 / 0 실패** (기존 102 + geometry 13 + calibration 12)
- 기존 102개 전부 그대로 통과 — 회귀 없음
- `deno check` 통과 (scanner engine, autotrader, scanner index, scalp 모듈 전체)
- `strategy != "SCALP"` 경로는 전부 미변경. TREND 모드와 백테스트 하니스는 v5.2.5 와 동일

---

## 5. 아직 검증되지 않은 것

- **`edgeBudget = 0.10` 은 가정입니다.** 신호가 실제로 10pp 의 초과승률을 주는지는
  아무도 모릅니다. 2-10 이 존재하는 이유가 그것이고, 표본 200건이 쌓여 프로필이
  승격되기 전까지 이 숫자는 검증된 값이 아닙니다.
- `resolveProbability` 는 해석적 근사입니다. `realized_resolve_rate` 가 쌓이면
  실측으로 교체하는 것이 맞습니다.
- 지정가 예약매도 상태기계는 **실거래소 대상으로 테스트되지 않았습니다.**
  기본값이 OFF 인 이유입니다. 켜신 뒤 첫 몇 건은 다음을 직접 확인하십시오:
  `TP_RESTED` → `POSITION_CLOSED` 흐름, 손절 시 `TP_CANCEL_*` 이벤트,
  거래소 미체결 주문 목록에 고아 주문이 남지 않는지.

## 6. 배포 순서

1. 마이그레이션 적용: `202607260004_scalp_geometry_v53.sql`
2. Supabase 함수 배포: `market-scanner`, `market-autotrader`
3. 대시보드에서 확인: `scalp_position_slots`, `scalp_edge_budget`, `scalp_max_holding_minutes`
4. **소액으로 먼저 1건.** 아래를 로그에서 확인:
   - `SCALP_GATE_BLOCK` 이벤트의 `scan_p_win` vs `order_p_win` 괴리 (신호 열화 정도)
   - `trading_positions.metadata.scalp_signal.geometry.binding_constraint` 분포
     (`COST_FLOOR` 일색이면 `scalp_edge_budget` 을 낮춰 배리어를 더 넓힐 것)
   - 진입이 6개 슬롯에 실제로 분산되는지
5. 게이트웨이 변경 없음 — 재배포 불필요
6. 캘리브레이션은 자동 스케줄(6시간마다). 먼저 `workflow_dispatch` 로 `dry_run=true`
   실행해 표본 수와 신뢰도 분포만 확인할 것
7. 예약매도는 위 전부가 안정된 뒤 `scalp_resting_tp = true` 로 별도 전환

### 즉시 권장

바이낸스 계정에 **BNB 25개 이상 보유 후 BNB 수수료 결제 활성화**.
왕복 0.20% → 0.15%, 필요 초과승률이 코드 변경 없이 내려감.
어떤 신호 개선보다 확실한 승률 개선 수단.


---

# v5.4.0 추가 변경

## 1. 30분 강제청산 제거 — 시계가 아니라 살아 있는 기대값으로 판단

30분은 수익 논리에서 나온 값이 아니라 최초 스캘핑 전환 때의 안전 타임아웃이었습니다.
고정 `+0.60%/-0.30%` 배리어 아래에서 "30분 안에 안 움직이면 신호가 끝났다"는 가정이었는데,
배리어가 종목별로 달라진 지금 이 가정은 성립하지 않습니다. 게다가 시간청산은
**승리도 패배도 아닌 결과에 왕복 수수료를 전액 지불**하는 가장 나쁜 청산입니다.

`_shared/scalp/hold.ts` 신설:

```
liveEdge = pWinNow × 남은상승여력 − (1 − pWinNow) × 남은하락위험 − 청산비용
```

이 식에서 중요한 세 가지:

1. **남은 상승여력은 진입가가 아니라 현재가 기준**입니다. 이미 간 만큼은 남아 있지 않습니다.
2. **청산비용은 편도만** 계산합니다. 진입 수수료·슬리피지는 매몰비용이고, 왕복을 다시
   물리면 아직 보유할 가치가 있는 포지션까지 정리됩니다.
3. **pWinNow 는 감쇠합니다.** 진입을 정당화한 호가 판독은 반감기가 분 단위이므로,
   시간이 지날수록 현재 호가가 가중치를 가져가고 진입 신호는 추정에서 빠집니다.
   시간은 여전히 중요하지만 **트리거가 아니라 신뢰도 감쇠 요소**입니다.

청산 조건:

| 조건 | 동작 |
|---|---|
| `liveEdge < 0` | 청산 |
| 예상 도달시간 경과 + `liveEdge < 최소보유기준` | 청산 (슬롯 점유 비용) |
| 위 조건이지만 T1 완료·수익 중 | **추적손절 강화** (즉시 투매 대신) |
| 호가 불균형 반전 2회 연속 확인 | 즉시 청산 |
| `SPOOF_LIKE` / `ASK_ABSORPTION` / `SUPPORT_BREAKDOWN` | 즉시 청산 |
| 4시간봉이 강한 하락으로 전환 | 즉시 청산 |
| 안전 TTL(기본 **6시간**) 초과 | 최후 방어선 |

두 개의 시계를 분리했습니다.

- `metadata.expected_resolution_at` — 변동성 기반 예상 도달시각. **청산 시각이 아니라
  판단 기준이 엄격해지는 시점**
- `max_holding_at` — 절대 백스톱. 장부 고착·모니터 장애 대비용이며 사용자가 설정

마이그레이션에 기존 열린 포지션의 `max_holding_at`을 새 TTL로 재설정하는 구문을
넣었습니다. 넣지 않으면 배포 직후 첫 모니터 틱에서 30분 기준으로 전부 강제청산됩니다.

## 2. 봇 자신의 주문을 수동 개입으로 오인하는 치명적 버그

원인:

```ts
// v5.2.5
const freeMissing = !totalMissing && actualFree + 1e-10 < expected * 0.999999;
```

봇 자신의 미체결 매도(예약 익절, 진행 중인 청산)가 기초자산을 **잠그면** `actualFree`가
`expected` 아래로 떨어집니다. 3회 확인 = **45초** 후 `manual_intervention_required = true`
→ **양 거래소 전 종목 신규진입 정지**. 봇이 정상 동작하다가 스스로 시스템을 멈추는 구조였습니다.
v5.3에서 추가한 예약 익절이 이 버그를 매번 유발했을 것입니다.

수정은 두 갈래입니다.

**(1) 봇 자신이 잠근 수량을 되돌려 계산**

`botLockedQuantities()` 신설 — 거래소 미체결 주문 중 봇 접두사(`tb-`)를 가진 매도 주문의
잔여 수량을 자산별로 합산해 `actualFree`에 더한 뒤 비교합니다. 게이트웨이 정규화 응답에
`side` / `market` / `remaining_volume`을 추가했고, 구버전 게이트웨이에서도 동작하도록
원본 페이로드 파싱 폴백을 뒀습니다. 조회 실패 시에는 **빈 맵을 반환**해서, 일시적 오류가
불일치를 만들어내지 않게 했습니다.

**(2) 정지 범위를 자산 단위로 축소**

| 상황 | v5.2.5 | v5.4 |
|---|---|---|
| 잠겨 있지만 계좌에 존재 (봇 주문) | 전체 정지 | **정상 — 불일치 아님** |
| 잠겨 있지만 계좌에 존재 (사용자 지정가) | 전체 정지 | 해당 자산만 일시정지, 나머지 정상 거래 |
| 자산이 계좌에서 실제로 사라짐 (1종목) | 전체 정지 | 해당 자산만 일시정지 |
| 여러 자산이 동시에 사라짐 | 전체 정지 | 전체 정지 (계정·키 문제 의심, 정당) |

`manual_asset_locks` 컬럼 신설. `enterCandidate`가 이 목록의 자산만 건너뜁니다.
마이그레이션에 **기존에 이 버그로 걸린 정지 플래그를 해제**하는 구문을 포함했습니다.

## 3. 수수료를 가정하지 않고 계정에서 조회

`FEE_PCT`는 정가(업비트 0.05% / 바이낸스 0.10%)를 하드코딩한 값이었습니다. 실제로는
BNB 수수료 결제·VIP 등급·프로모션에 따라 달라지고, **필요 승률은 비용의 직접 함수**이므로
가정된 수수료는 모든 배리어를 잘못 산정합니다.

- 게이트웨이 `fees` 액션 신설 (업비트 `/v1/orders/chance`, 바이낸스 `commissionRates`)
- 자동매매 엔진이 30분 캐시로 조회해 비용 모델·스캐너 요청에 실제 수수료 전달
- 조회 실패 또는 비정상 값(0~1% 범위 밖)이면 기존 상수로 폴백
- `scalp_use_live_fees = false` 로 끌 수 있음

## 4. 신규 설정 (`202607260006_...sql`)

| 컬럼 | 기본값 | 의미 |
|---|---|---|
| `scalp_safety_ttl_minutes` | 360 | 절대 백스톱. **정상 매매 규칙 아님** |
| `scalp_hold_alpha_half_life_minutes` | 12 | 진입 신호 신뢰도 반감기 |
| `scalp_hold_min_edge_after_expected` | 0.0005 | 예상 도달시간 경과 후 보유 유지에 필요한 기대값 |
| `scalp_hold_reversal_imbalance` | -0.25 | 호가 반전 판정 기준 |
| `scalp_hold_reversal_confirmations` | 2 | 반전 연속 확인 횟수 |
| `manual_asset_locks` | `[]` | 자산 단위 일시정지 목록 |
| `scalp_use_live_fees` | true | 계정 실제 수수료 조회 |

제약 추가: `max_new_entries_per_scan` 1~4 (대시보드 허용 범위 명시).

## 5. 검증

- 전체 테스트 **150개 통과 / 0 실패** (기존 102 + geometry 13 + calibration 12 + hold 14 + base-fee 9)
- 전체 `deno check` 통과. `market-performance/index.ts` 의 기존 타입 오류(`filter(Boolean)`
  가 null 을 좁히지 못함)도 함께 수정 — 런타임 동작은 불변
- `gateway/server.mjs` 문법 검사 통과

## 6. v5.4 배포 순서

1. **게이트웨이 먼저 재배포** (`fees` 액션, 주문 정규화 필드). 이걸 먼저 하지 않으면
   봇 잠금 인식이 폴백 경로로만 동작합니다
2. 마이그레이션 4개: `202607260004`, `202607260005`, `202607260006`, **`202607260007`**
3. Supabase 함수: `market-scanner`, `market-autotrader`, `scalp-calibration`
4. 대시보드에서 `scalp_safety_ttl_minutes`, `scalp_position_slots`,
   `max_new_entries_per_scan` 확인
5. 로그 확인 항목:
   - `ACCOUNT_LOCK_PARTIALLY_EXPLAINED` / `ASSET_SCOPED_PAUSE` 가 뜨는지 (정상 동작 신호)
   - `SAFETY_PAUSE` 가 더 이상 봇 자신의 주문으로 발생하지 않는지
   - `SCALP_HOLD_EXIT` 의 `reason` 분포 — `edge_negative` 가 대부분이면 정상,
     `orderbook_reversal_confirmed` 가 대부분이면 `scalp_hold_reversal_imbalance` 완화 검토
