# v7.6.0 — 바이낸스 USDⓈ-M 선물 레인

현물(Upbit KRW / Binance USDT)은 그대로 두고, **바이낸스 USDⓈ-M 무기한 선물**을 세 번째
거래소로 추가했습니다.

- **진입 로직은 현물과 완전히 동일합니다.** 같은 스캐너, 같은 LOB 진입 게이트, 롱 전용.
- **매도 조건만 바뀝니다.** 그리고 가격이 아니라 **증거금 기준 수익률(ROE)** 로 기술됩니다.

## 매도 규칙

| 단계 | 조건 (증거금 기준) | 동작 |
| --- | --- | --- |
| 1차 | ROE **+15%** | 절반 익절 |
| 1차 | ROE **−12%** | 절반 손절 |
| 잔여 | ROE **+30%** | 전량 익절 |
| 잔여 | 한 번이라도 마이너스였다면 | **플러스 전환 즉시 전량 매도** (+30% 무시) |

기본 레버리지는 **3배**이며, 3배에서 위 게이트는 가격 기준 **−4% / +5% / +10%** 와 정확히
같습니다. 즉 현물 레인의 숫자와 동일한 모양이지만, 레버리지를 바꾸면 가격 게이트가 함께
움직입니다. 이것이 임계값을 가격이 아닌 증거금 기준으로 적어 둔 이유입니다.

"한 번이라도 마이너스였다면"은 **래치(latch)** 입니다. 잔여 물량이 수수료 차감 후 한 번이라도
마이너스로 관측되면 그 상태가 기록되고, 그 뒤로는 +30%가 아니라 "플러스 전환"만 기다립니다.
1차가 −12%로 손절된 경우에는 그 시점에 곧바로 래치가 걸립니다.

여기서 말하는 "플러스"는 **남은 절반 자신의** 순손익입니다. 남은 물량의 진입 원가와 양쪽
수수료를 모두 회수하고도 남는 금액이 있을 때만 매도가 승인됩니다. (현물 레인의 회복 모드는
포지션 **전체** 순손익이 플러스가 될 때까지 기다립니다 — 이 차이는 의도된 것으로, 요청하신
문구가 "남은 절반"을 가리키기 때문입니다. 전체 기준으로 바꾸길 원하시면 알려주세요.)

## 사이징

배분 로직은 현물과 같습니다. 다만 배분된 금액은 **증거금**이고, 거래소에 나가는 주문
금액은 `증거금 × 레버리지` 입니다. 같은 자본을 넣고 3배의 익스포저를 갖게 되므로,
가격 −4%가 투입 자본의 −12%가 되는 것이 맞습니다.

계좌 자본과 익스포저 회계도 증거금 기준입니다. 선물 지갑의 자본은 거래소가 보고하는
`totalMarginBalance`(미실현 손익 포함)를 그대로 씁니다.

## 켜는 방법

기본값은 **꺼짐**입니다. 대시보드 "운용" 카드에 `BINANCE USDⓈ-M` 카드가 추가되어 있고,
거기서 신규 매수 스위치 / 레버리지 / 운용금 / 보호금을 설정합니다.

필요한 환경변수(게이트웨이):

| 변수 | 기본값 | 설명 |
| --- | --- | --- |
| `BINANCE_FUTURES_BASE_URL` | `https://fapi.binance.com` | USDⓈ-M 호스트 |
| `BINANCE_FUTURES_DEFAULT_LEVERAGE` | `3` | 주문에 레버리지가 안 실려 왔을 때의 기본값 |
| `BINANCE_FUTURES_ORDER_GATEWAY_URL` | (미설정 시 바이낸스 게이트웨이) | 선물 전용 게이트웨이 |
| `BINANCE_FUTURES_GATEWAY_SHARED_SECRET` | (미설정 시 바이낸스 시크릿) | 위 게이트웨이용 시크릿 |
| `BINANCE_FUTURES_FEE_PER_SIDE_PCT` | `0.05` | 첫 실계좌 조회 전 기본 수수료 |

API 키는 현물과 같은 키를 쓰지만, 해당 계정에 **USDⓈ-M 선물이 활성화**되어 있어야 합니다.

## 안전 장치

- **롱 전용.** 게이트웨이는 SELL 주문에 항상 `reduceOnly`(헤지 모드에서는 `positionSide=LONG`)를
  붙입니다. 이 경로로는 숏을 열 수 없습니다.
- **출금·이체 라우트 없음.** 추가된 계정 쓰기 권한은 심볼별 레버리지 설정 하나뿐입니다.
- **레버리지 상한 20배.** 게이트웨이, 엔진, DB 제약이 각각 독립적으로 강제합니다.
- **레버리지는 포지션에 기록됩니다.** 운용 중에 설정을 바꿔도, 이미 열린 포지션은 열릴 때의
  레버리지로 청산됩니다.
- DB 트리거(`guard_residual_sell_order_v751`)가 엔진과 무관하게 ROE 임계값을 재검증합니다.
  엔진이 잘못된 가격에 매도를 시도하면 주문 자체가 거부됩니다.

### 알아두셔야 할 위험

잔여 물량에는 **퍼센트 손절이 없습니다.** 규칙 3을 그대로 구현했기 때문에, 마이너스로 내려간
잔여 물량은 플러스 전환까지 무기한 보유합니다. 3배 레버리지에서 가격이 약 −33% 움직이면
청산(liquidation)입니다. 잔여 물량은 포지션의 절반이므로 실제 여유는 그보다 크지만,
"플러스 전환까지 기다린다"는 규칙은 구조적으로 청산 위험을 감수합니다. 잔여 물량에도
하드 손절을 두길 원하시면 말씀해 주세요.

## 스캔 신호

선물 레인은 바이낸스 **현물 스캔이 만든 BUY 후보를 그대로 복제**해서 씁니다("진입 로직은
현물과 동일"). 복제된 후보는 `exchange = 'binance_futures'` 로 별도 저장되므로 성과와 학습은
레인별로 분리됩니다. 반면 **호가·심도·체결·1분봉은 전부 선물 시장에서 직접** 읽습니다 —
게이트웨이가 거래소별로 라우팅하기 때문입니다.

## 마이그레이션

`supabase/migrations/20260810010000_binance_futures_lane_v760.sql`

- 모든 `exchange` 체크 제약과 마켓 라우트 제약에 `binance_futures` 추가
- `trading_settings`: `binance_futures_enabled`(기본 false), `binance_futures_leverage`(기본 3),
  `binance_futures_allocation_mode` / `_allocation_usdt` / `_reserve_usdt`
- `trading_positions.leverage` 컬럼 (기본 1)
- `position_exit_levels_v760()`: 거래소와 레버리지로부터 손절/목표가를 계산
- `enforce_residual_exit_position_policy_v751()` / `guard_residual_sell_order_v751()` 확장
- 엔진 리비전을 `7.6.0-BINANCE-FUTURES` 로 정렬

마이그레이션 말미의 검증 블록은 **현물 레인의 5 / −4 / 10 이 그대로인지**와 3배 선물이
−4 / +5 / +10 으로 매핑되는지를 직접 확인하고, 하나라도 어긋나면 배포를 실패시킵니다.

## 검증

- `deno task check` — 통과
- `deno task test` — 548 통과 (신규 25개 포함)
- `node --test gateway/server.test.mjs` — 23 통과
- 마이그레이션 전체 체인을 로컬 PostgreSQL 16에 재생하여 적용 확인
- 매도 가드를 실제 DB에서 기능 검증: 1차 트랜치 +6% ROE 차단 / +15%·−12% 승인(절반으로 절삭),
  잔여 +29.9% 차단·+30.0% 승인, 회복 모드는 진입가 이하 차단·수수료 회수 시점 승인,
  현물 레인은 이전과 동일하게 동작
