# v7.4.0 — 통계 검정 결과의 코드 반영

기준 분석: 바이낸스 실거래 20건 (승리 3건), 24시간 구간
반영 원칙: **검정으로 확인되지 않은 진입 변수는 실거래 판단을 바꿀 수 없다.**

---

## 1. 분석이 실제로 말한 것

| 구분 | 결과 |
|---|---|
| 진입 시점 변수 8종 | 다중검정 보정 후 전부 q > 0.05 — 유의하지 않음 |
| p_target | 원 p = 0.023, 보정 q = 0.188 — 후보일 뿐 확정 아님 |
| MFE / MAE | q < 0.001 — 매우 유의하지만 **진입 후** 변수 |
| 보유시간 | p = 0.186 — 유의하지 않음 |
| 재진입 | Mann–Whitney p = 0.275 — 유의하지 않음 |

즉 "지금 어떤 임계값을 올려라"가 결론이 아니다. 결론은 **현재 기록된 진입 변수만으로는 수익 거래와 손실 거래를 구별할 수 없다**는 것이고, 그보다 더 근본적인 문제는 코드가 이 질문을 던질 방법 자체를 가지고 있지 않았다는 점이다. 지금까지 모든 임계값 변경은 눈으로 본 표에 근거했다.

이번 변경은 특정 임계값을 조정하지 않는다. **검정을 시스템의 상시 구성요소로 만든다.**

---

## 2. 추가된 것

### `_shared/lob/feature-significance.ts` — 검정 엔진

외부에서 한 번 수행한 분석을 코드 안에서 재현하고, 매 캘리브레이션 주기마다 실거래 데이터로 다시 계산한다.

- **Spearman 순위상관** — 표본이 작고 수익률 꼬리가 두꺼우므로 정규성을 어디에서도 가정하지 않는다
- **순열검정 p값** — 20행 표본이 뒷받침할 수 없는 점근 근사를 쓰지 않는다. n ≤ 8은 전수 열거, 그 이상은 데이터에서 유도한 시드로 결정론적 몬테카를로. 같은 구간을 다시 돌리면 같은 p값이 나온다
- **Benjamini–Hochberg FDR** — **패밀리 내부에서만** 보정한다. 의도적으로 설계된 진입 변수 8종이 한 가설군, 넓은 미시구조 스윕이 다른 가설군이다. 섞으면 스윕의 폭이 진입 변수의 q값을 희석시킨다
- **Leave-one-out 민감도** — 거래 한 건을 빼면 부호가 뒤집히는 상관은 그 거래의 산물이지 변수의 성질이 아니다
- **Mann–Whitney U** — 재진입 대조군용. 7건 대 13건에서는 정규근사가 부정확하므로 라벨을 재표집한다
- **PRE_ENTRY / POST_ENTRY 구조적 분리** — 아래 3절
- **거래소 코호트 분리** — 풀링을 옵션으로 제공하지 않는다. 수수료 체계·틱 구조·호가 단위가 다르므로 통합 상관은 거래소 구성비를 측정하게 된다

### `_shared/lob/feature-admission.ts` — 증거 → 권한 매핑

검정 결과를 실행 경로가 따를 수 있는 형태로 바꾼다. 권한은 세 단계다.

| 권한 | 허용 범위 |
|---|---|
| `NONE` | 로그·대시보드 표시만. 정렬·필터·사이징 불가 |
| `TIE_BREAK` | 동점 후보의 순서만 결정. 후보를 제거하지 않고 임계값을 움직이지 않으므로, 노이즈로 판명나도 비용이 없다 |
| `GATE` | 진입 임계값을 조이거나 포지션 크기를 조정 가능 |

**모든 변수의 기본값은 `NONE`이다.** 권한은 그럴듯함이 아니라 측정으로 부여된다. EV·hotness·confidence는 모두 이 엔진이 공들여 계산하는 값이지만, 이번 검정에서 어느 것도 `GATE` 권한을 얻지 못했다.

`GATE` 승격 조건 (전부 충족):
- q < 0.05
- 코호트 표본 ≥ 30건
- 승/패 각 클래스 ≥ 8건 — 20건에 승리 3건이면 소수 클래스가 추정치 전체를 좌우한다
- leave-one-out 부호 안정
- |ρ| ≥ 0.30

### `20260804170000_v740_statistical_feature_evidence.sql`

- `lob_feature_evidence` — 추가 전용 검정 이력. 코호트별·실행별 1행. 덮어쓰지 않는다. 임계값 변경은 그것을 정당화한 정확한 리포트로 추적 가능해야 한다
- `trading_settings.lob_feature_admission` — 런타임이 읽는 현재 권한 맵. 기본값은 빈 배열, 즉 모든 변수 `NONE`
- `guard_lob_feature_admission()` 트리거 — 아래 3절

---

## 3. 구조적으로 막은 것: MFE·MAE의 사후 사용

분석 4절이 지적한 대로 MFE와 MAE는 유의하지만 진입 전에 알 수 없다. 수익률과 강하게 상관되는 것은 구조상 당연하다 — 유리하게 크게 움직인 거래가 이익으로 끝난다.

이것을 진입 발견으로 보고하는 오류를 통계가 아니라 **구조로** 차단한다.

1. `TRADE_PATH_FEATURES`는 `causality: "POST_ENTRY"`로 선언된다. `feature-admission.ts`는 q값과 무관하게 `NONE`을 부여한다 — `TIE_BREAK`조차 주지 않는다
2. DB 트리거가 `mfe_bps` / `mae_bps` / `held_seconds`에 `NONE` 이외의 권한을 부여하는 권한 맵을 **거부**한다. 미래의 리팩터링이 조용히 이 값들을 권한 맵으로 흘려보내면 강한 신호처럼 보이지만 실제로는 lookahead다. 모든 호출자가 이를 기억하리라 믿는 대신 DB가 거절한다

대신 분석이 요구한 방향("MFE·MAE를 사전에 예측하는 호가·체결·캔들 변수가 별도로 필요")을 자동화했다. 리포트는 세 가지 질문을 같은 행에 대해 동시에 돌린다.

1. 진입 변수가 실현 수익률을 설명하는가
2. 사전 미시구조 변수가 실현 수익률을 설명하는가
3. **사전 미시구조 변수가 MFE·MAE 경로를 설명하는가** — 강한 경로 상관이 주문 전 행동으로 바뀔 수 있는 유일한 경로

3번에서 유의한 변수가 나오면 리포트에 `PRE_ENTRY_EXCURSION_PREDICTORS`로 표시된다. 단, 그것만으로는 권한을 주지 않는다. 초과폭은 수익이 아니다.

미시구조 패밀리는 손으로 나열하지 않고 기록된 피처 벡터에서 발견한다 (커버리지 80% 이상, 최대 60개). 엔진에 피처가 추가되면 다음 검정부터 자동으로 포함된다.

---

## 4. 확정할 수 없는 주장으로 취급하는 것

분석에 따라, 아래는 현재 데이터로 주장할 수 없으므로 코드에서 임계값 근거로 쓰지 않는다.

- EV가 높아서 / score가 높아서 / confidence가 높아서 수익이 났다
- RR이 높아서 수익이 났다, 예상 비용이 높아서 손실이 났다
- 손절 폭이나 목표 폭이 손익을 결정했다
- 오래 보유해서 / 빨리 청산해서 손실이 발생했다
- 재진입이 손실을 유발했다

`p_target`은 `CANDIDATE` = `TIE_BREAK`로 분류된다. 정렬에는 참여하고 임계값은 움직이지 못한다. 분석의 표현("현재 데이터에서 수익과 관련될 가능성이 가장 높은 진입 변수지만, 20건만으로 유의성이 확정되지는 않았다")과 정확히 일치하는 처리다.

재진입 대조는 리포트에 계속 남는다. 주장이 결국 입증되거나 결국 폐기되도록 하기 위해서이며, 조용히 통념이 되는 것을 막기 위해서다.

---

## 5. 기존 임계값에 실제로 일어난 변화

`gate-config.ts`의 `minEvBps`는 "EV는 결과와 측정된 관계가 없다"는 **주석**으로 0에 고정돼 있었다. 맞는 말이었지만 강제력이 없었다 — 다음 수정이 다시 올리는 것을 막을 수단이 없었다.

이제 측정된 권한 맵에서 해석된다.

```
minEvBps = mayHardenGate(admission, "ev_lower_bound_bps")
  ? clamp(operator_setting, 0, LOB_MAX_EVIDENCE_GATED_EV_BPS)
  : 0
```

`entry.ts`의 `minPrimaryPatternConfidence` / `minHotnessScore` / `minEvBps`도 같은 방식으로 바뀌었다.

**오늘의 동작은 그대로다.** 어떤 변수도 `GATE` 권한이 없으므로 셋 다 0으로 해석된다. 바뀐 것은 두 가지다.

- 증거 없이 게이트를 되살리는 수정이 불가능해졌다
- 미래의 측정이 게이트를 되살리는 데 코드 수정이 필요 없어졌다

권한을 얻은 값도 사전에 검토된 상한(`LOB_MAX_EVIDENCE_GATED_EV_BPS = 5`) 안에 들어온다. 증거가 도착한 순간 설정 행에 우연히 들어 있던 값을 그대로 가져오지 않는다.

### 아직 연결하지 않은 것 (의도적)

스캐너와 오토트레이더 호출부에는 권한 맵을 아직 주입하지 않았다. 두 단계는 반드시 동일한 게이트를 유도해야 하는데 (`gate-config.ts` 상단 주석이 경고하는, BUY 후보만 쌓이고 체결은 없던 버그), 스캐너는 `trading_settings`를 직접 읽지 않고 요청 본문으로 오버라이드를 받는다. 한쪽만 연결하면 나중에 함정이 된다.

현재는 양쪽 모두 권한 맵 없이 호출되어 동일하게 0으로 해석된다 — 기존 동작과 완전히 같다. 실제 주입은 **어떤 변수가 실제로 `GATE` 권한을 얻은 시점**에 양쪽을 함께 연결하는 후속 작업이다. 지금 연결할 이유가 없다: 권한을 가진 변수가 하나도 없다.

---

## 6. 운영 중 읽는 법

`lob-calibration`은 매 실행마다 리포트를 계산해 응답의 `feature_evidence`에 담고, `lob_feature_evidence`에 적재하고, `trading_settings.lob_feature_admission`에 권한 맵을 게시한다. `dry_run=1`로 쓰기 없이 확인할 수 있다.

주요 노트 값:

| 노트 | 의미 |
|---|---|
| `NO_PRE_ENTRY_FEATURE_SEPARATES_OUTCOMES` | 진입 임계값은 운영자 설정 그대로 유지 — 현재 상태 |
| `SAMPLE_BELOW_SIGNIFICANCE_FLOOR` | 표본이 30건 미만. 어떤 변수도 승격 불가 |
| `OUTCOME_CLASSES_UNBALANCED` | 다변량·로지스틱 회귀 불가. 분석이 지적한 그대로 |
| `PATH_VARIABLES_SIGNIFICANT_BUT_UNUSABLE` | MFE/MAE가 유의하지만 권한 없음 |
| `PRE_ENTRY_EXCURSION_PREDICTORS` | 경로를 예측하는 사전 변수 발견. 수익률에 대해서는 아직 미검증 |

---

## 7. 검증

새 테스트 27건. 전체 스위트 498건 통과.

검정 엔진 자체를 검증하는 테스트에는 실제 분석 수치가 들어 있다.

- BH 보정이 `[0.000021, 0.000663, 0.186]` → `[0.000063, 0.0009945, 0.186]`을 재현 (MFE/MAE/보유시간 패밀리)
- 진입 변수 8종 패밀리에서 원 p = 0.023 → q ≈ 0.184로 밀려나 유의하지 않게 됨을 확인
- 20건·승리 3건 형태의 종단 테스트에서 `gateFeatures`가 비어 있음을 확인
- q ≈ 0.000063인 MFE가 `NONE` 권한에 머무름을 확인

기존 실패 7건은 이번 변경 이전부터 존재한다 (HEAD 기준선과 동일). 그중 `gate-config.test.ts`의 "missing settings fall back to the measured-safe gate"는 `maxGainerRank === 3`을 기대하는데 기본값이 10으로 바뀐 오래된 단언이다. 이번 작업 범위 밖이라 손대지 않았다.
