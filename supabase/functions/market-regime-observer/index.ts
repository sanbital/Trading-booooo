// Trading-booooo market regime observer v2.
// Observation/dashboard only. It does not write trading settings, entries, exits, sizing, leverage, or orders.

const MODEL_REVISION="MARKET-REGIME-OBSERVER-v2-C01-FULLMARKET";
const CANDIDATE_ID="C01_LEGACY_SHAPE_V1";
const RESEARCH_RUN_ID="384c6838-b9d3-4677-9da2-644dc54e2a98";
const HORIZONS=[30,120,360] as const;
const SUPABASE_URL=requiredEnv("SUPABASE_URL").replace(/\/$/,"");
const SERVICE_KEY=requiredEnv("SUPABASE_SERVICE_ROLE_KEY");
const DASHBOARD_TOKEN=(Deno.env.get("DASHBOARD_ACCESS_TOKEN")||Deno.env.get("LEARNING_ACCESS_TOKEN")||"").trim();
const ALLOWED_ORIGIN=(Deno.env.get("ALLOWED_ORIGINS")||"*").split(",")[0].trim()||"*";
const BS="https://api.binance.com",BF="https://fapi.binance.com",UP="https://api.upbit.com";
const CORS_HEADERS={"Access-Control-Allow-Origin":ALLOWED_ORIGIN,"Access-Control-Allow-Headers":"content-type, x-autotrade-token, x-regime-token, apikey","Access-Control-Allow-Methods":"POST, OPTIONS"};
type Json=Record<string,any>;type Regime="STRONG_BULL"|"BULL"|"NEUTRAL"|"RISK_OFF";type Venue="binance_spot"|"binance_futures"|"upbit_spot";type Ticker={market:string;price:number;ret24:number;quoteVolume:number};type Breadth={sample_size:number;positive_fraction:number;clipped_mean_pct:number;gain_tail_fraction:number;loss_tail_fraction:number;score:number};type Snapshot={observedAt:string;allPrices:Record<string,number>;benchmarkPrices:Record<string,number>;sampleSize:number;features:Json;bullScore:number;confidence:number;regime:Regime};
function requiredEnv(n:string){const v=(Deno.env.get(n)||"").trim();if(!v)throw new Error(`missing ${n}`);return v}function finite(v:unknown,d=0){const n=Number(v);return Number.isFinite(n)?n:d}function clamp(v:number,lo=0,hi=100){return Math.min(hi,Math.max(lo,Number.isFinite(v)?v:lo))}function mean(xs:number[]){const a=xs.filter(Number.isFinite);return a.length?a.reduce((s,x)=>s+x,0)/a.length:0}function stddev(xs:number[]){const a=xs.filter(Number.isFinite);if(!a.length)return 0;const m=mean(a);return Math.sqrt(mean(a.map(x=>(x-m)**2)))}function returnPct(n:number,p:number){return n>0&&p>0?(n/p-1)*100:0}function momentumScore(r:number,f:number){return clamp(50+(r/Math.max(.01,f))*50)}function regimeOf(s:number):Regime{return s>=72?"STRONG_BULL":s>=58?"BULL":s>=42?"NEUTRAL":"RISK_OFF"}function actualRegime(s:number):Regime{return s>=75?"STRONG_BULL":s>=60?"BULL":s>=40?"NEUTRAL":"RISK_OFF"}function ordinal(r:Regime){return({RISK_OFF:0,NEUTRAL:1,BULL:2,STRONG_BULL:3})[r]}function direction(r:Regime):-1|0|1{return r==="RISK_OFF"?-1:r==="NEUTRAL"?0:1}function constantTimeEqual(l:string,r:string){const e=new TextEncoder(),a=e.encode(l),b=e.encode(r),n=Math.max(a.length,b.length);let d=a.length^b.length;for(let i=0;i<n;i++)d|=(a[i]||0)^(b[i]||0);return d===0}function json(b:unknown,s=200){return new Response(JSON.stringify(b),{status:s,headers:{...CORS_HEADERS,"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"}})}
async function db(path:string,init:RequestInit={}){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{...init,headers:{apikey:SERVICE_KEY,Authorization:`Bearer ${SERVICE_KEY}`,"Content-Type":"application/json",...(init.headers||{})}});const t=await r.text();if(!r.ok)throw new Error(`database ${r.status}: ${t.slice(0,500)}`);return t?JSON.parse(t):null}async function regimeTokenAllowed(req:Request){const got=(req.headers.get("x-regime-token")||"").trim();if(got.length<32)return false;const rows=await db("edge_internal_tokens?name=eq.market-regime-observer&select=token&limit=1").catch(()=>[]);const want=String(rows?.[0]?.token||"").trim();return want.length>=32&&constantTimeEqual(got,want)}function dashboardAllowed(req:Request){const got=(req.headers.get("x-autotrade-token")||"").trim();return DASHBOARD_TOKEN.length>=32&&got.length>0&&constantTimeEqual(DASHBOARD_TOKEN,got)}
async function fetchJson(url:string,attempts=3){let last="unknown";for(let i=0;i<attempts;i++){const c=new AbortController(),tm=setTimeout(()=>c.abort(),15000);try{const r=await fetch(url,{signal:c.signal,headers:{accept:"application/json","user-agent":"Trading-booooo-regime-observer-v2/1"}}),t=await r.text();if(r.ok)return t?JSON.parse(t):null;last=`HTTP ${r.status}: ${t.slice(0,180)}`;if(![418,429].includes(r.status)&&r.status<500)throw new Error(last)}catch(e){last=e instanceof Error?e.message:String(e);if(i+1>=attempts)throw e}finally{clearTimeout(tm)}await new Promise(r=>setTimeout(r,400*2**i))}throw new Error(last)}
async function binanceTickers(v:"binance_spot"|"binance_futures"):Promise<Ticker[]>{const fut=v==="binance_futures",base=fut?BF:BS,[info,tickers]=await Promise.all([fetchJson(`${base}${fut?"/fapi/v1/exchangeInfo":"/api/v3/exchangeInfo"}`),fetchJson(`${base}${fut?"/fapi/v1/ticker/24hr":"/api/v3/ticker/24hr"}`)]),active=new Set((Array.isArray(info?.symbols)?info.symbols:[]).filter((r:any)=>r?.status==="TRADING"&&r?.quoteAsset==="USDT"&&(fut?r?.contractType==="PERPETUAL":r?.isSpotTradingAllowed!==false)).map((r:any)=>String(r.symbol)));return(Array.isArray(tickers)?tickers:[]).filter((r:any)=>active.has(String(r.symbol))).map((r:any)=>({market:String(r.symbol),price:finite(r.lastPrice),ret24:finite(r.priceChangePercent),quoteVolume:finite(r.quoteVolume)})).filter((r:Ticker)=>r.price>0)}async function upbitTickers():Promise<Ticker[]>{const info=await fetchJson(`${UP}/v1/market/all?isDetails=true`),markets=(Array.isArray(info)?info:[]).map((r:any)=>String(r.market||"")).filter((m:string)=>m.startsWith("KRW-")).sort(),out:Ticker[]=[];for(let i=0;i<markets.length;i+=80){const chunk=markets.slice(i,i+80),rows=await fetchJson(`${UP}/v1/ticker?markets=${encodeURIComponent(chunk.join(","))}`);for(const r of Array.isArray(rows)?rows:[])out.push({market:String(r.market||""),price:finite(r.trade_price),ret24:finite(r.signed_change_rate)*100,quoteVolume:finite(r.acc_trade_price_24h)})}return out.filter(r=>r.market&&r.price>0)}function priceMap(v:Venue,rows:Ticker[]){const p=v==="binance_spot"?"BS:":v==="binance_futures"?"BF:":"UP:",o:Record<string,number>={};for(const r of rows)o[p+r.market]=r.price;return o}
function breadthFromReturns(returns:number[],h:30|1440):Breadth{const a=returns.filter(Number.isFinite);if(!a.length)return{sample_size:0,positive_fraction:.5,clipped_mean_pct:0,gain_tail_fraction:0,loss_tail_fraction:0,score:50};const clip=h===30?5:20,tail=h===30?1.5:8,scale=h===30?1.2:8,pos=a.filter(x=>x>0).length/a.length,gain=a.filter(x=>x>=tail).length/a.length,loss=a.filter(x=>x<=-tail).length/a.length,clipped=mean(a.map(x=>Math.max(-clip,Math.min(clip,x)))),score=.52*pos*100+.33*momentumScore(clipped,scale)+.15*clamp(50+(gain-loss)*90);return{sample_size:a.length,positive_fraction:pos,clipped_mean_pct:clipped,gain_tail_fraction:gain,loss_tail_fraction:loss,score:clamp(score)}}
function compareVenue(base:Record<string,number>,now:Record<string,number>,prefix:string,h:30|120|360){const clip=h===30?5:h===120?8:12,tail=h===30?1.5:h===120?3:5,raw:number[]=[];for(const[k,p]of Object.entries(base||{})){if(!k.startsWith(prefix))continue;const n=finite(now[k]);if(p>0&&n>0)raw.push(returnPct(n,p))}const pos=raw.length?raw.filter(x=>x>0).length/raw.length:.5,gain=raw.length?raw.filter(x=>x>=tail).length/raw.length:0,loss=raw.length?raw.filter(x=>x<=-tail).length/raw.length:0,clipped=raw.length?mean(raw.map(x=>Math.max(-clip,Math.min(clip,x)))):0;return{sample_size:raw.length,positive_fraction:pos,clipped_mean_pct:clipped,gain_tail_fraction:gain,loss_tail_fraction:loss,score:h===30?clamp(.52*pos*100+.33*momentumScore(clipped,1.2)+.15*clamp(50+(gain-loss)*90)):null}}
async function priorV2(nowMs:number){const earliest=new Date(nowMs-35*60000).toISOString(),latest=new Date(nowMs-25*60000).toISOString(),rows=await db(`market_regime_observations?model_revision=eq.${encodeURIComponent(MODEL_REVISION)}&observed_at=gte.${encodeURIComponent(earliest)}&observed_at=lte.${encodeURIComponent(latest)}&select=observed_at,liquid_prices&order=observed_at.asc&limit=5`).catch(()=>[]);if(!rows?.length)return null;return[...rows].sort((a:any,b:any)=>Math.abs((nowMs-Date.parse(a.observed_at))/60000-30)-Math.abs((nowMs-Date.parse(b.observed_at))/60000-30))[0]}
async function researchFallback30(){const latest=await db(`regime_v2_feature_aggregates?run_id=eq.${RESEARCH_RUN_ID}&horizon_minutes=eq.30&select=bucket&order=bucket.desc&limit=1`).catch(()=>[]),bucket=latest?.[0]?.bucket;if(!bucket)return{score:50,bucket:null};const rows=await db(`regime_v2_feature_aggregates?run_id=eq.${RESEARCH_RUN_ID}&horizon_minutes=eq.30&bucket=eq.${encodeURIComponent(bucket)}&select=*`).catch(()=>[]),scores=(rows||[]).map((r:any)=>{const n=Math.max(1,finite(r.sample_count)),pos=finite(r.positive_count)/n,gain=finite(r.gain_tail_count)/n,loss=finite(r.loss_tail_count)/n,clipped=finite(r.sum_clipped_return)/n;return.52*pos*100+.33*momentumScore(clipped,1.2)+.15*clamp(50+(gain-loss)*90)});return{score:scores.length?mean(scores):50,bucket}}
function klineReturns(rows:any[],up=false){const b=(Array.isArray(rows)?rows:[]).map((r:any)=>up?{t:Date.parse(String(r.candle_date_time_utc||"")+"Z"),c:finite(r.trade_price)}:{t:finite(r?.[0]),c:finite(r?.[4])}).filter((r:any)=>r.t>0&&r.c>0).sort((a:any,b:any)=>a.t-b.t),last=b.at(-1)?.c||0,ret=(back:number)=>b.length>back&&last>0?returnPct(last,b[b.length-1-back].c):0;return{r30:ret(1),r120:ret(4),r360:ret(12),r1440:ret(48)}}
async function benchmarkScore(){const boundary=Math.floor(Date.now()/1800000)*1800000,end=boundary-1,defs:Array<[Venue,string,string,number]>=[["binance_spot","BTCUSDT","BTC",1],["binance_spot","ETHUSDT","ETH",.75],["binance_spot","SOLUSDT","SOL",.5],["binance_futures","BTCUSDT","BTC",1],["binance_futures","ETHUSDT","ETH",.75],["binance_futures","SOLUSDT","SOL",.5],["upbit_spot","KRW-BTC","BTC",1],["upbit_spot","KRW-ETH","ETH",.75],["upbit_spot","KRW-SOL","SOL",.5]],rows=await Promise.all(defs.map(async([v,m,a,w])=>{let raw:any;if(v==="upbit_spot")raw=await fetchJson(`${UP}/v1/candles/minutes/30?market=${encodeURIComponent(m)}&count=49&to=${encodeURIComponent(new Date(boundary).toISOString())}`);else{const base=v==="binance_futures"?BF:BS,path=v==="binance_futures"?"/fapi/v1/klines":"/api/v3/klines";raw=await fetchJson(`${base}${path}?symbol=${m}&interval=30m&limit=49&endTime=${end}`)}const r=klineReturns(raw,v==="upbit_spot"),score=.16*momentumScore(r.r30,1)+.28*momentumScore(r.r120,2.5)+.31*momentumScore(r.r360,4.5)+.25*momentumScore(r.r1440,8);return{venue:v,market:m,asset:a,weight:w,score,...r}})),den=rows.reduce((s,r)=>s+r.weight,0);return{score:den?rows.reduce((s,r)=>s+r.score*r.weight,0)/den:50,rows}}
function benchmarkPriceMap(bs:Ticker[],bf:Ticker[],up:Ticker[]){const o:Record<string,number>={};for(const[prefix,rows,names]of[["BS:",bs,new Set(["BTCUSDT","ETHUSDT","SOLUSDT"])],["BF:",bf,new Set(["BTCUSDT","ETHUSDT","SOLUSDT"])],["UP:",up,new Set(["KRW-BTC","KRW-ETH","KRW-SOL"])]]as any)for(const r of rows as Ticker[])if(names.has(r.market))o[prefix+r.market]=r.price;return o}function weightedBenchmarkForward(base:Record<string,number>,now:Record<string,number>){let total=0,den=0;for(const[k,p]of Object.entries(base||{})){const n=finite(now[k]);if(!(p>0&&n>0))continue;const w=k.includes("BTC")?1:k.includes("ETH")?.75:.5;total+=returnPct(n,p)*w;den+=w}return den?total/den:0}
async function collectSnapshot():Promise<Snapshot>{const observedAt=new Date().toISOString(),nowMs=Date.parse(observedAt),[bs,bf,up,bench,prior]=await Promise.all([binanceTickers("binance_spot"),binanceTickers("binance_futures"),upbitTickers(),benchmarkScore(),priorV2(nowMs)]);if(bs.length<100||bf.length<100||up.length<40)throw new Error(`universe too small spot=${bs.length} futures=${bf.length} upbit=${up.length}`);const maps={...priceMap("binance_spot",bs),...priceMap("binance_futures",bf),...priceMap("upbit_spot",up)},b24={binance_spot:breadthFromReturns(bs.map(x=>x.ret24),1440),binance_futures:breadthFromReturns(bf.map(x=>x.ret24),1440),upbit_spot:breadthFromReturns(up.map(x=>x.ret24),1440)},g1440=mean(Object.values(b24).map(x=>x.score));let b30:any=null,g30=50,fallback:any=null;if(prior?.liquid_prices&&Object.keys(prior.liquid_prices).length){const parts:any={};for(const[prefix,venue]of[["BS:","binance_spot"],["BF:","binance_futures"],["UP:","upbit_spot"]]as const)parts[venue]=compareVenue(prior.liquid_prices,maps,prefix,30);if(parts.binance_spot.sample_size>=80&&parts.binance_futures.sample_size>=80&&parts.upbit_spot.sample_size>=40){g30=mean(Object.values(parts).map((x:any)=>x.score));b30=parts}}if(!b30){fallback=await researchFallback30();g30=fallback.score}const raw=.50*bench.score+.30*g30+.20*g1440,bullScore=clamp(50+(raw-50)*.88),components=[bench.score,g30,g1440],nearest=Math.min(Math.abs(bullScore-42),Math.abs(bullScore-58),Math.abs(bullScore-72),20),agreement=1-clamp(stddev(components)/35,0,1),confidence=clamp((.45+.35*(nearest/20)+.20*agreement)*(b30?1:.82),.35,.95);return{observedAt,allPrices:maps,benchmarkPrices:benchmarkPriceMap(bs,bf,up),sampleSize:bs.length+bf.length+up.length,bullScore,confidence,regime:regimeOf(bullScore),features:{candidate_id:CANDIDATE_ID,evidence_run_id:RESEARCH_RUN_ID,source:"BINANCE_SPOT_FUTURES_UPBIT_FULL_ACTIVE_UNIVERSE",universe:{binance_spot:bs.length,binance_futures:bf.length,upbit_spot:up.length,total:bs.length+bf.length+up.length},benchmark:{score:bench.score,markets:bench.rows},breadth_30m:b30,breadth_30m_fallback:b30?null:{score:g30,research_bucket:fallback?.bucket||null},breadth_24h:b24,component_scores:{benchmark:bench.score,breadth_30m:g30,breadth_24h:g1440,raw},weights:{benchmark:.50,breadth_30m:.30,breadth_24h:.20,sensitivity:.88},thresholds:{risk_off_below:42,bull:58,strong_bull:72},training_ground_truth_thresholds:{risk_off_below:40,bull:60,strong_bull:75},trading_influence:false}}}
async function persistObservation(s:Snapshot){const bucketMs=Math.floor(Date.parse(s.observedAt)/300000)*300000+2000,bucket=new Date(bucketMs).toISOString(),inserted=await db("market_regime_observations?on_conflict=observation_bucket",{method:"POST",headers:{Prefer:"resolution=ignore-duplicates,return=representation"},body:JSON.stringify({observation_bucket:bucket,observed_at:s.observedAt,model_revision:MODEL_REVISION,predicted_regime:s.regime,bull_score:s.bullScore,confidence:s.confidence,sample_size:s.sampleSize,features:s.features,benchmark_prices:s.benchmarkPrices,liquid_prices:s.allPrices,trading_influence:false})});if(inserted?.[0])return inserted[0];const rows=await db(`market_regime_observations?observation_bucket=eq.${encodeURIComponent(bucket)}&select=*&limit=1`);return rows?.[0]||{}}
async function evaluateDue(s:Snapshot){const nowMs=Date.parse(s.observedAt);let inserted=0;for(const h of HORIZONS){const earliest=new Date(nowMs-(h+12)*60000).toISOString(),latest=new Date(nowMs-h*60000).toISOString(),obs=await db(`market_regime_observations?model_revision=eq.${encodeURIComponent(MODEL_REVISION)}&observed_at=gte.${encodeURIComponent(earliest)}&observed_at=lte.${encodeURIComponent(latest)}&select=id,observed_at,predicted_regime,bull_score,benchmark_prices,liquid_prices&order=observed_at.asc`).catch(()=>[]);for(const o of obs||[]){if(!o.liquid_prices||Object.keys(o.liquid_prices).length<100)continue;const parts=[compareVenue(o.liquid_prices,s.allPrices,"BS:",h),compareVenue(o.liquid_prices,s.allPrices,"BF:",h),compareVenue(o.liquid_prices,s.allPrices,"UP:",h)];if(parts[0].sample_size<80||parts[1].sample_size<80||parts[2].sample_size<40)continue;const pos=mean(parts.map(x=>x.positive_fraction)),clipped=mean(parts.map(x=>x.clipped_mean_pct)),benchForward=weightedBenchmarkForward(o.benchmark_prices||{},s.benchmarkPrices),scale=Math.sqrt(120/h),realized=clamp(50+scale*benchForward*14+scale*clipped*18+(pos-.5)*50),actual=actualRegime(realized),pred=String(o.predicted_regime)as Regime,actualH=(nowMs-Date.parse(o.observed_at))/60000,result=await db("market_regime_outcomes?on_conflict=observation_id,horizon_minutes",{method:"POST",headers:{Prefer:"resolution=ignore-duplicates,return=representation"},body:JSON.stringify({observation_id:o.id,horizon_minutes:h,evaluated_at:s.observedAt,actual_horizon_minutes:actualH,realized_regime:actual,realized_bull_score:realized,exact_match:pred===actual,directional_match:direction(pred)===direction(actual),regime_distance:Math.abs(ordinal(pred)-ordinal(actual)),forward_benchmark_pct:benchForward,forward_median_pct:clipped,forward_positive_fraction:pos,sample_size:parts.reduce((z,x)=>z+x.sample_size,0),details:{model_revision:MODEL_REVISION,candidate_id:CANDIDATE_ID,predicted_bull_score:finite(o.bull_score),validation_basis:"forward full-market Binance spot + futures + Upbit breadth and 9 benchmark prices",trading_influence:false}})});if(result?.length)inserted++}}return inserted}
function summarizeAccuracy(rows:Json[],h:number){const a=rows.filter(r=>finite(r.horizon_minutes)===h&&r?.details?.model_revision===MODEL_REVISION);if(!a.length)return{samples:0,exact_rate:null,directional_rate:null,avg_regime_distance:null};return{samples:a.length,exact_rate:a.filter(r=>r.exact_match===true).length/a.length,directional_rate:a.filter(r=>r.directional_match===true).length/a.length,avg_regime_distance:a.reduce((s,r)=>s+finite(r.regime_distance),0)/a.length}}
async function statusPayload(){const since=new Date(Date.now()-90*86400000).toISOString(),[latest,recent,outcomes]=await Promise.all([db(`market_regime_observations?model_revision=eq.${encodeURIComponent(MODEL_REVISION)}&select=id,observation_bucket,observed_at,model_revision,predicted_regime,bull_score,confidence,sample_size,features,trading_influence,created_at&order=observed_at.desc&limit=1`).catch(()=>[]),db(`market_regime_observations?model_revision=eq.${encodeURIComponent(MODEL_REVISION)}&select=id,observed_at,predicted_regime,bull_score,confidence&order=observed_at.desc&limit=8`).catch(()=>[]),db(`market_regime_outcomes?evaluated_at=gte.${encodeURIComponent(since)}&select=*&order=evaluated_at.desc&limit=5000`).catch(()=>[])]),accuracy:Json={};for(const h of HORIZONS)accuracy[String(h)]=summarizeAccuracy(outcomes||[],h);const outcomeBy=new Map<string,Json[]>();for(const o of outcomes||[]){if(o?.details?.model_revision!==MODEL_REVISION)continue;const k=String(o.observation_id||""),a=outcomeBy.get(k)||[];a.push(o);outcomeBy.set(k,a)}return{ok:true,model_revision:MODEL_REVISION,candidate_id:CANDIDATE_ID,trading_influence:false,weights_locked:true,learning_phase:"LIVE_FORWARD_VALIDATION",minimum_samples_before_review:200,latest:latest?.[0]||null,accuracy,recent:(recent||[]).map((r:Json)=>({...r,outcomes:(outcomeBy.get(String(r.id))||[]).sort((a,b)=>finite(a.horizon_minutes)-finite(b.horizon_minutes))})),backtest_test:{ground_truth:"same full-market target for V1 and V2",v2:{30:{samples:287,exact_rate:.4111,directional_rate:.4774,avg_regime_distance:.7596},120:{samples:284,exact_rate:.4225,directional_rate:.5106,avg_regime_distance:.7394},360:{samples:276,exact_rate:.3116,directional_rate:.4493,avg_regime_distance:.8478},weighted_score:.47051},v1:{30:{samples:287,exact_rate:.4077,directional_rate:.4564,avg_regime_distance:.7526},120:{samples:284,exact_rate:.3732,directional_rate:.4401,avg_regime_distance:.7852},360:{samples:276,exact_rate:.3116,directional_rate:.3949,avg_regime_distance:.8913},weighted_score:.43812},relative_weighted_improvement:.07393},note:"Market regime V2 is dashboard/observation only and is not read by entry or exit logic."}}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:CORS_HEADERS});if(req.method!=="POST")return json({error:"POST only"},405);try{const body=await req.json().catch(()=>({}))as Json,action=String(body.action||"status").toLowerCase();if(action==="status"){const internal=await regimeTokenAllowed(req).catch(()=>false);if(!dashboardAllowed(req)&&!internal)return json({error:"unauthorized"},401);return json(await statusPayload())}if(!(await regimeTokenAllowed(req)))return json({error:"unauthorized"},401);if(!["tick","observe","evaluate"].includes(action))return json({error:"unsupported action"},400);const s=await collectSnapshot();let evaluated=0,observation:Json|null=null;if(action==="tick"||action==="evaluate")evaluated=await evaluateDue(s);if(action==="tick"||action==="observe")observation=await persistObservation(s);return json({ok:true,action,evaluated,observation:observation?{id:observation.id,observed_at:observation.observed_at,predicted_regime:observation.predicted_regime,bull_score:observation.bull_score,confidence:observation.confidence}:null,status:await statusPayload()})}catch(e){console.error("market regime observer v2 failed",e);return json({error:e instanceof Error?e.message:String(e)},500)}});
