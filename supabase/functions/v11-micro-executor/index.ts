// @ts-nocheck
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.57.4";
const REVISION="V11-LONG-REGIME-1.0.1",OBSERVER_REVISION="MARKET-REGIME-OBSERVER-v2-C01-HYSTERESIS-v1-FULLMARKET",PROTOCOL="8.0.0-P10-DONCHIAN-SLOW4R",MARGIN=40,LEV=3,NOTIONAL=120,SNAP_MAX=90000,SPREAD_MAX=25,SIGNAL_MAX=120000;
function res(s,b){return new Response(JSON.stringify(b),{status:s,headers:{"content-type":"application/json","cache-control":"no-store"}})}function N(v,d=0){const x=Number(v);return Number.isFinite(x)?x:d}function rec(v){return v&&typeof v==="object"&&!Array.isArray(v)?v:{}}function eq(a,b){if(a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}function floorStep(v,s){if(!(v>0&&s>0))return 0;const p=Math.min(12,Math.max(0,Math.ceil(-Math.log10(s))+2));return Number((Math.floor((v+s*1e-9)/s)*s).toFixed(p))}function cid(p,x){return`tb-${p}-${String(x).toLowerCase().replace(/[^a-z0-9]/g,"").slice(0,24)}`.slice(0,36)}
const env=n=>(Deno.env.get(n)||"").trim(),GW=env("BINANCE_FUTURES_ORDER_GATEWAY_URL").replace(/\/$/,"")||env("BINANCE_ORDER_GATEWAY_URL").replace(/\/$/,"")||env("ORDER_GATEWAY_URL").replace(/\/$/,""),SEC=env("BINANCE_FUTURES_GATEWAY_SHARED_SECRET")||env("BINANCE_GATEWAY_SHARED_SECRET")||env("GATEWAY_SHARED_SECRET");
async function hmac(s,m){const k=await crypto.subtle.importKey("raw",new TextEncoder().encode(s),{name:"HMAC",hash:"SHA-256"},false,["sign"]),g=await crypto.subtle.sign("HMAC",k,new TextEncoder().encode(m));return[...new Uint8Array(g)].map(x=>x.toString(16).padStart(2,"0")).join("")}
async function gateway(cmd,tm=20000){if(!GW||!SEC)throw new Error("GATEWAY_CONFIG");const x=cmd.action==="create_order"?{...cmd,engine_version:PROTOCOL}:cmd,raw=JSON.stringify({exchange:"binance_futures",...x}),ts=String(Date.now()),nonce=crypto.randomUUID(),sig=await hmac(SEC,`${ts}\n${nonce}\n${raw}`),c=new AbortController,t=setTimeout(()=>c.abort(),tm);try{const r=await fetch(`${GW}/v1/command`,{method:"POST",signal:c.signal,headers:{"content-type":"application/json","x-gateway-ts":ts,"x-gateway-nonce":nonce,"x-gateway-signature":sig},body:raw}),txt=await r.text();let d;try{d=txt?JSON.parse(txt):null}catch{d={raw:txt}}if(!r.ok||!d?.ok)throw new Error(`GW_${r.status}:${d?.error||txt}`);return d.result}finally{clearTimeout(t)}}
function fill(p){const o=p?.order??p??{},f=p?.fill??{},q=Math.max(0,N(f.executedVolume??f.executed_quantity??o.executed_volume??o.executedQty)),a=Math.max(0,N(f.averagePrice??f.average_price??o.average_price??o.avgPrice));return{status:String(o?.status??p?.status??"UNKNOWN").toUpperCase(),exchangeOrderId:o?.exchange_order_id==null?o?.orderId==null?null:String(o.orderId):String(o.exchange_order_id),qty:q,avg:a,fee:Math.max(0,N(f.paidFeeQuote??f.paidFee??o.paid_fee??o.commission)),raw:p}}
function active(p){return(Array.isArray(p?.positions)?p.positions:[]).filter(x=>Math.abs(N(x?.quantity??x?.positionAmt??x?.position_amount))>1e-12)}function sym(p){return String(p?.market??p?.symbol??"").toUpperCase()}function qty(p){return Math.abs(N(p?.quantity??p?.positionAmt??p?.position_amount))}
async function auth(db,req){const p=(req.headers.get("x-v10-executor-token")||"").trim();const t=await db.from("edge_internal_tokens").select("token").eq("name","v10-lane-executor").maybeSingle();return!t.error&&p&&t.data?.token&&eq(p,String(t.data.token))}
function route(v){const x=String(v||"").toUpperCase();return x==="RISK_OFF"?"BEAR":x==="NEUTRAL"?"RANGE":x==="BULL"||x==="STRONG_BULL"?"BULL":"CASH"}
async function market(db){const o=await db.from("market_regime_observations").select("id,observed_at,predicted_regime,bull_score,confidence").eq("model_revision",OBSERVER_REVISION).eq("trading_influence",true).order("observed_at",{ascending:false}).limit(1).maybeSingle();if(o.error)throw new Error(`OBSERVER:${o.error.message}`);const age=o.data?Date.now()-Date.parse(o.data.observed_at):Infinity;return{route:age<=12*60000?route(o.data?.predicted_regime):"CASH",ageMs:age,observer:o.data||null}}
async function snap(db){const s=await db.from("trading_account_snapshots").select("captured_at,available_quote,positions,positions_complete").eq("exchange","binance_futures").order("captured_at",{ascending:false}).limit(1).maybeSingle();if(s.error||!s.data)throw new Error("SNAPSHOT_MISSING");const age=Date.now()-Date.parse(s.data.captured_at);if(s.data.positions_complete!==true||age>SNAP_MAX)throw new Error(`SNAPSHOT_INVALID:${age}`);return{...s.data,ageMs:age}}
async function circuit(db,r){await db.from("v11_long_regime_runtime").update({circuit_open:true,circuit_reason:String(r).slice(0,500),last_error:String(r).slice(0,1000),updated_at:new Date().toISOString()}).eq("singleton",true)}
async function audit(db,p,b,a,action,reason,details={}){await db.from("v11_long_regime_decisions").insert({revision:REVISION,position_id:p?.id||null,observed_regime:details.marketRoute||null,active_lane_before:b||null,active_lane_after:a||null,action,reason,details})}
async function openEntry(db,s){const[sn,pf,q,i]=await Promise.all([snap(db),gateway({action:"p10_portfolio"}),gateway({action:"quote",market:s.symbol}),gateway({action:"symbol_info",market:s.symbol})]),ext=active(pf);if(ext.length){await circuit(db,`MICRO_EXTERNAL_EXPOSURE:${ext.map(sym).join(",")}`);throw new Error("EXTERNAL_POSITION")}const bid=N(q?.best_bid),ask=N(q?.best_ask),sp=bid>0&&ask>0?(ask/bid-1)*10000:999;if(!(bid>0&&ask>0&&sp<=SPREAD_MAX))throw new Error(`ENTRY_SPREAD:${sp}`);const f=rec(s.features),ref=N(f.referenceClose),gap=ref>0?Math.abs(ask/ref-1)*10000:999;if(gap>15)throw new Error(`MICRO_ENTRY_GAP_BPS:${gap}`);const live=N(pf?.available_quote,NaN),avail=Math.min(N(sn.available_quote),live);if(!Number.isFinite(live)||avail<MARGIN+NOTIONAL*.001)throw new Error(`ENTRY_MARGIN:${avail}`);const step=N(i?.quantity_step??i?.step_size),min=Math.max(1,N(i?.min_notional,5)),amount=floorStep(NOTIONAL/ask,step);if(!(amount>0&&amount*ask>=min))throw new Error("QTY_INVALID");const id=cid("v11m",s.id),rp={action:"create_order",leverage:LEV,order:{market:s.symbol,side:"BUY",type:"MARKET",quantity:amount,identifier:id,position_side:"LONG",position_effect:"OPEN"},wait_for_final_ms:4000},oi=await db.from("v11_long_regime_orders").insert({revision:REVISION,signal_id:s.id,position_id:null,symbol:s.symbol,intent:"OPEN_LONG",reason:"MICRO_ENTRY",client_order_id:id,requested_quantity:amount,state:"PLANNED",request_payload:{...rp,quantity_step:step,margin_usdt:MARGIN,leverage:LEV,spread_bps:sp,gap_bps:gap}}).select("*").single();if(oi.error)throw new Error(`ORDER_INTENT:${oi.error.message}`);let submitted=false;try{submitted=true;const raw=await gateway(rp),z=fill(raw);if(!(z.qty>0&&z.avg>0)){await db.from("v11_long_regime_orders").update({state:"RECONCILIATION_FAILED",response_payload:raw,reject_reason:`NO_FILL:${z.status}`,updated_at:new Date().toISOString()}).eq("id",oi.data.id);await db.from("v11_long_regime_signals").update({status:"ORDERED",updated_at:new Date().toISOString()}).eq("id",s.id);await circuit(db,`MICRO_ENTRY_AMBIGUOUS:${z.status}`);throw new Error(`NO_FILL:${z.status}`)}const stopPct=N(f.stopPct,.0035),maxMin=N(f.maxHoldMin,s.lane==="BEAR"?8:12),now=new Date(),stop=z.avg*(1-stopPct),pos=await db.from("v11_long_regime_positions").insert({signal_id:s.id,revision:REVISION,entry_lane:s.lane,active_lane:s.lane,transition_from:null,symbol:s.symbol,side:"LONG",original_quantity:z.qty,remaining_quantity:z.qty,entry_price:z.avg,entry_at:now.toISOString(),entry_atr:N(f.atr,z.avg),entry_bb_pos:N(f.bbPos,0),hard_stop_price:stop,hard_deadline:new Date(now.getTime()+maxMin*60000).toISOString(),active_since:now.toISOString(),active_ref_bb:N(f.bbPos,0),active_target_delta:null,t1_completed:false,peak_price:z.avg,state:"OPEN",realized_pnl_usdt:-z.fee,entry_fee_usdt:z.fee,metadata:{executionMode:"MICRO",targetPct:N(f.targetPct,.0045),stopPct,maxHoldMin,microPattern:f.microPattern||"SWEEP_RECLAIM",entryFeatures:f,lastAppliedOrderId:oi.data.id,entryOrderId:z.exchangeOrderId||id}}).select("*").single();if(pos.error)throw new Error(`POSITION:${pos.error.message}`);await db.from("v11_long_regime_orders").update({state:"FILLED",exchange_order_id:z.exchangeOrderId,response_payload:raw,position_id:pos.data.id,updated_at:new Date().toISOString()}).eq("id",oi.data.id);await db.from("v11_long_regime_signals").update({status:"FILLED",position_id:pos.data.id,updated_at:new Date().toISOString()}).eq("id",s.id);return{entered:true,positionId:pos.data.id,symbol:s.symbol,entryPrice:z.avg,quantity:z.qty,stopPrice:stop,targetPrice:z.avg*(1+N(f.targetPct,.0045)),maxHoldMin:maxMin}}catch(e){if(submitted)throw e;await db.from("v11_long_regime_orders").update({state:"REJECTED",reject_reason:String(e).slice(0,500),updated_at:new Date().toISOString()}).eq("id",oi.data.id);throw e}}
async function closePos(db,p,reason){const[pf,i]=await Promise.all([gateway({action:"p10_portfolio"}),gateway({action:"symbol_info",market:p.symbol})]),match=active(pf).filter(x=>sym(x)===p.symbol.toUpperCase());if(match.length!==1){await circuit(db,`MICRO_POSITION_MISMATCH:${p.symbol}:${match.length}`);throw new Error("POSITION_MISMATCH")}const step=N(i?.quantity_step??i?.step_size),amount=floorStep(Math.min(N(p.remaining_quantity),qty(match[0])),step);if(!(amount>0))throw new Error("EXIT_QTY_ZERO");const id=cid("v11mx",`${p.id}${crypto.randomUUID()}`),rp={action:"create_order",order:{market:p.symbol,side:"SELL",type:"MARKET",quantity:amount,identifier:id,position_side:"LONG",position_effect:"CLOSE"},wait_for_final_ms:4000},oi=await db.from("v11_long_regime_orders").insert({revision:REVISION,signal_id:p.signal_id,position_id:p.id,symbol:p.symbol,intent:"CLOSE_LONG",reason,client_order_id:id,requested_quantity:amount,state:"PLANNED",request_payload:{...rp,quantity_step:step}}).select("*").single();if(oi.error)throw new Error(`EXIT_INTENT:${oi.error.message}`);const raw=await gateway(rp),z=fill(raw);if(!(z.qty>0&&z.avg>0)){await db.from("v11_long_regime_orders").update({state:"RECONCILIATION_FAILED",response_payload:raw,reject_reason:`EXIT_NO_FILL:${z.status}`,updated_at:new Date().toISOString()}).eq("id",oi.data.id);await circuit(db,`MICRO_EXIT_AMBIGUOUS:${z.status}`);throw new Error(`EXIT_NO_FILL:${z.status}`)}const pnl=(z.avg-N(p.entry_price))*z.qty-z.fee,real=N(p.realized_pnl_usdt)+pnl;await db.from("v11_long_regime_positions").update({remaining_quantity:0,state:"CLOSED",realized_pnl_usdt:real,exit_price:z.avg,exit_reason:reason,closed_at:new Date().toISOString(),metadata:{...rec(p.metadata),lastAppliedOrderId:oi.data.id,lastExitOrderId:z.exchangeOrderId||id,lastExitReason:reason},updated_at:new Date().toISOString()}).eq("id",p.id);await db.from("v11_long_regime_orders").update({state:"FILLED",exchange_order_id:z.exchangeOrderId,response_payload:raw,updated_at:new Date().toISOString()}).eq("id",oi.data.id);await db.from("v11_long_regime_signals").update({status:"CLOSED",updated_at:new Date().toISOString()}).eq("id",p.signal_id);return{closed:true,exitPrice:z.avg,realizedPnlUsdt:real}}
async function manage(db,p,m){const q=await gateway({action:"quote",market:p.symbol}),bid=N(q?.best_bid),ask=N(q?.best_ask);if(!(bid>0&&ask>0))throw new Error("EXIT_QUOTE_INVALID");const before=p.active_lane,stop=N(p.hard_stop_price),meta=rec(p.metadata);if(bid<=stop){await audit(db,p,before,before,"FULL_CLOSE","MICRO_HARD_STOP",{marketRoute:m.route,bid,stop});return{action:"CLOSE",reason:"MICRO_HARD_STOP",result:await closePos(db,p,"MICRO_HARD_STOP")}}if(m.route==="BULL"){const deadline=new Date(Date.now()+30*86400000).toISOString(),up=await db.from("v11_long_regime_positions").update({active_lane:"BULL",transition_from:before,active_since:new Date().toISOString(),active_target_delta:null,hard_deadline:deadline,metadata:{...meta,executionMode:"GRADUATED_BULL",microGraduatedAt:new Date().toISOString()},updated_at:new Date().toISOString()}).eq("id",p.id).select("*").single();if(up.error)throw new Error(`GRADUATE:${up.error.message}`);await audit(db,p,before,"BULL","SWITCH_POLICY","MICRO_TO_BULL_GRADUATE",{marketRoute:m.route,bid,hardStopPreserved:stop});return{action:"GRADUATE_BULL",positionId:p.id}}
let pos=p;if(["RANGE","BEAR"].includes(m.route)&&m.route!==before){const maxMin=m.route==="BEAR"?8:12,targetPct=m.route==="BEAR"?.005:.0045,newStop=Math.max(stop,N(p.entry_price)*(1-.0035)),now=new Date(),up=await db.from("v11_long_regime_positions").update({active_lane:m.route,transition_from:before,active_since:now.toISOString(),hard_stop_price:newStop,hard_deadline:new Date(now.getTime()+maxMin*60000).toISOString(),metadata:{...meta,executionMode:"MICRO",targetPct,maxHoldMin:maxMin},updated_at:now.toISOString()}).eq("id",p.id).select("*").single();if(up.error)throw new Error(`MICRO_SWITCH:${up.error.message}`);pos=up.data;await audit(db,p,before,m.route,"SWITCH_POLICY",`MICRO_${before}_TO_${m.route}`,{marketRoute:m.route,newStop,targetPct,maxHoldMin})}
const mm=rec(pos.metadata),targetPct=N(mm.targetPct,pos.active_lane==="BEAR"?.005:.0045),target=N(pos.entry_price)*(1+targetPct),deadline=Date.parse(pos.hard_deadline);if(bid>=target){const r=`${pos.active_lane}_MICRO_TARGET`;await audit(db,pos,pos.active_lane,pos.active_lane,"FULL_CLOSE",r,{marketRoute:m.route,bid,target,targetPct});return{action:"CLOSE",reason:r,result:await closePos(db,pos,r)}}if(Date.now()>=deadline){const r=`${pos.active_lane}_MICRO_TIME_EXIT`;await audit(db,pos,pos.active_lane,pos.active_lane,"FULL_CLOSE",r,{marketRoute:m.route,bid,deadline:pos.hard_deadline});return{action:"CLOSE",reason:r,result:await closePos(db,pos,r)}}await audit(db,pos,pos.active_lane,pos.active_lane,"HOLD","MICRO_HOLD",{marketRoute:m.route,bid,target,stop:pos.hard_stop_price,deadline:pos.hard_deadline});return{action:"HOLD",lane:pos.active_lane,bid,target,stop:N(pos.hard_stop_price),deadline:pos.hard_deadline}}
async function run(db){const rt=await db.from("v11_long_regime_runtime").select("*").eq("singleton",true).single();if(rt.error||!rt.data)throw new Error("RUNTIME");if(rt.data.live_enabled!==true||rt.data.circuit_open===true)return{ok:true,skipped:"RUNTIME_NOT_LIVE",runtime:rt.data};const op=await db.from("v11_long_regime_positions").select("*").eq("state","OPEN").limit(2);if(op.error)throw new Error("POSITIONS");if((op.data||[]).length>1){await circuit(db,"MULTIPLE_V11_POSITIONS");throw new Error("MULTIPLE_POSITIONS")}const p=op.data?.[0]||null,m=await market(db);if(p){if(p.active_lane==="BULL")return{ok:true,revision:REVISION,skipped:"BULL_OWNED_BY_TREND_EXECUTOR",marketState:m,position:{id:p.id,symbol:p.symbol}};const pf=await gateway({action:"p10_portfolio"}),ext=active(pf),matches=ext.filter(x=>sym(x)===p.symbol.toUpperCase());if(ext.length!==1||matches.length!==1){await circuit(db,`MICRO_EXCHANGE_MISMATCH:${ext.length}:${matches.length}`);throw new Error("EXCHANGE_MISMATCH")}const action=await manage(db,p,m);await db.from("v11_long_regime_runtime").update({last_success_at:new Date().toISOString(),last_error:null,last_exit_at:action.action==="CLOSE"?new Date().toISOString():rt.data.last_exit_at,updated_at:new Date().toISOString()}).eq("singleton",true);return{ok:true,revision:REVISION,marketState:m,position:{id:p.id,symbol:p.symbol,entryLane:p.entry_lane,activeLane:p.active_lane},action}}
if(!["RANGE","BEAR"].includes(m.route))return{ok:true,revision:REVISION,marketState:m,entry:{entered:false,reason:"MICRO_ROUTE_INACTIVE"}};const since=new Date(Date.now()-SIGNAL_MAX).toISOString(),sg=await db.from("v11_long_regime_signals").select("*").eq("revision",REVISION).eq("status","NEW").eq("lane",m.route).gte("entry_bar_at",since).order("entry_bar_at",{ascending:false}).limit(10);if(sg.error)throw new Error(`SIGNALS:${sg.error.message}`);const s=(sg.data||[]).find(x=>rec(x.features).executionMode==="MICRO");if(!s){await db.from("v11_long_regime_runtime").update({last_success_at:new Date().toISOString(),last_error:null,updated_at:new Date().toISOString()}).eq("singleton",true);return{ok:true,revision:REVISION,marketState:m,entry:{entered:false,reason:"NO_FRESH_MICRO_SIGNAL"}}}const cl=await db.from("v11_long_regime_signals").update({status:"CLAIMED",updated_at:new Date().toISOString()}).eq("id",s.id).eq("status","NEW").select("*").maybeSingle();if(cl.error)throw new Error(`CLAIM:${cl.error.message}`);if(!cl.data)return{ok:true,revision:REVISION,entry:{entered:false,reason:"CLAIM_RACE"}};try{const entry=await openEntry(db,cl.data);await db.from("v11_long_regime_runtime").update({last_success_at:new Date().toISOString(),last_error:null,last_entry_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("singleton",true);return{ok:true,revision:REVISION,marketState:m,entry}}catch(e){const msg=e instanceof Error?e.message:String(e);const pending=await db.from("v11_long_regime_orders").select("id").eq("signal_id",s.id).in("state",["SUBMITTED","RECONCILIATION_FAILED"]).limit(1);if(!pending.data?.length)await db.from("v11_long_regime_signals").update({status:"REJECTED",reject_reason:msg.slice(0,500),updated_at:new Date().toISOString()}).eq("id",s.id);throw e}}
Deno.serve(async req=>{if(req.method!=="POST")return res(405,{ok:false,error:"POST_ONLY"});const U=env("SUPABASE_URL"),K=env("SUPABASE_SERVICE_ROLE_KEY"),db=createClient(U,K,{auth:{persistSession:false,autoRefreshToken:false}});if(!(await auth(db,req)))return res(401,{ok:false,error:"UNAUTHORIZED"});const body=await req.json().catch(()=>({}));try{if(String(body.mode||"").toLowerCase()==="preflight"){const[rt,m,sn,pf,q,i]=await Promise.all([db.from("v11_long_regime_runtime").select("*").eq("singleton",true).single(),market(db),snap(db),gateway({action:"p10_portfolio"}),gateway({action:"quote",market:String(body.symbol||"BTCUSDT")}),gateway({action:"symbol_info",market:String(body.symbol||"BTCUSDT")})]);return res(200,{ok:true,revision:REVISION,runtime:rt.data,marketState:m,snapshotAgeMs:sn.ageMs,availableUsdt:Math.min(N(sn.available_quote),N(pf?.available_quote)),externalPositions:active(pf).map(x=>({symbol:sym(x),quantity:qty(x)})),quote:q,symbolInfo:{step:i?.quantity_step??i?.step_size,minNotional:i?.min_notional}})}return res(200,await run(db))}catch(e){const msg=e instanceof Error?e.message:String(e);try{await db.from("v11_long_regime_runtime").update({last_error:msg.slice(0,1000),updated_at:new Date().toISOString()}).eq("singleton",true)}catch{}return res(500,{ok:false,revision:REVISION,error:msg})}});