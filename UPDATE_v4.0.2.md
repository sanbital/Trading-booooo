# Trading-booooo v4.0.2

## 수정 사항

- 상단 브랜드 버전이 `v3.0.0`으로 고정 표시되던 문제를 수정했습니다.
- `styles.css`, `config.js`, `app.js`의 캐시 버전 문자열을 `4.0.2`로 갱신했습니다.
- 스캔 완료 후 API 응답의 `engine_version`으로 상단 버전을 자동 갱신합니다.
- 엔진 메타 버전을 `4.0.2`로 통일했습니다.

기존 v4.0.1의 스캔·포워드 학습·자동 승격 로직은 그대로 유지됩니다.
