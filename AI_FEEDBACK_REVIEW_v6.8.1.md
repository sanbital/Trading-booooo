# 다른 AI 피드백 검토 — v6.8.1 반영 판단

## 총평

가장 타당한 지적은 GPT/Perplexity 계열의 **P0 원장·라벨 무결성 우선**이다.
실데이터에서 잔량 가치가 손실로 기록되는 사례가 확인됐으므로 v6.8.1에서 즉시
수정했다.

반면 Gemini가 제안한 v7.0 예시 코드는 개념 초안으로는 참고할 수 있지만,
그대로 실전 코드에 넣기에는 기존 기능과 중복되고 통계적으로 위험한 부분이 많다.

## 반영

### 1. 학습 전에 주문·체결·수수료 라벨을 정확하게 만든다

완전 반영했다. 잔량·기준자산 수수료·경제적 실현손익을 포지션과 학습 원장에
명시적으로 저장한다. 검증 불가능한 옛 거래는 학습에서 제외한다.

### 2. 단일 점수보다 Pareto 비열등 승격을 사용한다

방향은 맞지만 v6.8.0에 이미 구현돼 있다. 순손익, 승률, 거래 완료율, 자본회전,
주문금액, MDD, 거래소×패턴 구성 표준화를 동시에 통과해야 하므로 새로 중복
구현하지 않았다.

### 3. 실체결 비용·지연을 폐루프로 반영한다

방향은 맞다. 다만 현재 코드에는 이미 다음이 존재한다.

- `trading_latency_samples`
- 실측 p95 지연 비용
- 양방향 스트레스 슬리피지 비용 모델
- 실제 메이커 체결률 보정
- live fee 측정
- 주문 직전 alpha decay 및 EV 재검사

따라서 별도 `live_execution_feedback` 테이블을 중복 생성하지 않았다.

## 보류 또는 거절

### 1. 15초/45초 고정 Alpha Velocity 강제청산

현재 엔진은 이미 실시간 호가 반전, 체결압력 반전, bid depth 소멸, spread 폭증,
live edge 음전, 학습된 grace/confirmation, 180초 기본 timeout을 사용한다.

모든 종목을 15초/45초로 고정하면 거래소·종목별 체결속도와 변동성 차이를 무시하고
수수료성 회전만 증가시킬 수 있다. 추후 독립 CHALLENGER 정책으로 검증할 수는
있지만 즉시 CHAMPION 로직으로 넣지 않았다.

### 2. 최근 10~20건 승률이 50% 미만이면 목표·손절 자동 축소

거절했다. 필요한 승률은 목표/손절 기하와 비용에 따라 달라지므로 50%는 보편적
기준이 아니다. 표본 10건은 변동성이 너무 커서 파라미터가 흔들리고 손절만
잦아질 수 있다.

### 3. 하드코딩된 pWin 가중치

`book_imbalance * 0.12`, `breakout * 0.08`, velocity ±0.04 같은 값은 첨부
실데이터로 교정된 계수가 아니다. 기존 확률 교정·LCB·holdout 체계를 우회하므로
채택하지 않았다.

### 4. 제안 SQL의 latency 계산

`EXTRACT(MILLISECOND FROM filled_at - requested_at)`은 총 지연시간이 아니라 interval의
밀리초 필드만 반환할 수 있다. 신규 스키마가 필요하다면
`extract(epoch from (filled_at-requested_at))*1000`을 써야 한다. 현재는 기존
latency ledger가 있어 신규 테이블 자체가 불필요하다.

### 5. 모든 지표 동시 극대화 보장

시장 비용 때문에 수익률·승률·회전율의 동시 최대화는 보장할 수 없다. 올바른 목표는
한 지표의 개선으로 다른 지표의 악화를 숨기지 못하도록 비열등 승격 계약을 두는
것이며, v6.8의 방식이 이에 더 가깝다.

## 최종 결정

이번 릴리스는 신호를 더 엄격하게 만들지 않는다. 먼저 잘못된 손실 라벨을 제거하고,
기존 v6.8 Pareto 검증이 깨끗한 데이터로 CHAMPION/CHALLENGER를 비교하도록 만든다.
속도 기반 청산은 이후 충분한 실거래 cohort를 가진 별도 challenger로 실험하는 것이
안전하다.
